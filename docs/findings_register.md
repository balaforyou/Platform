# Findings Register — Running Log (reconciled 31 Jul 2026)

**Purpose:** One live place tracking every gap, bug, or open decision surfaced during review. Modeled on the BCC governance-trail discipline: nothing gets fixed silently and forgotten, nothing stays open without being visible.

**Process note, added after a real incident:** IDs F-018/F-019 were independently reused twice for unrelated findings, causing the more significant findings under those IDs to be silently lost from the register. Restored below under F-022/F-023. **Going forward: the reviewer alone assigns and tracks IDs — the coding agent reports findings back in plain description, never picks or writes an ID itself.**

**Process note, added 2 Aug 2026:** a heuristic UX/business-fit report's summary claimed "zero leaks anywhere" across all captured evidence, but one of its own cited JSON artifacts (`admin-01-resources.json`) directly contradicted this (`"allCaps": ["PER_PERSON"]`). The underlying issue was trivial (a legitimate pricing-enum value, not a real leak), but the blanket claim itself was inaccurate and wasn't caught before being used as supporting evidence. Lesson: summary claims in any report — not just code walkthroughs — need to be checked against their own cited evidence before being trusted, the same discipline already applied to technical walkthroughs under rule 8.

**Process note, added 15 Aug 2026 — retiring two unused IDs.** A sweep of the register found three numbers with no row. F-095 has since been assigned a real finding. The remaining two are **retired permanently and must not be reissued**, because reusing a number that already carries meaning elsewhere is precisely the F-018/F-019 incident above:

- **F-021** — referenced but never defined. F-039's entry cites "F-020/F-021's ID collisions", so the number was consumed by the original collision incident this note block records. It stays retired as history; the dangling reference is intentional and should not be resolved by inventing a row for it.
- **F-089** — referenced nowhere at all, a clean gap between F-088 and F-090. Most likely a skipped number, retired rather than recycled so no future finding inherits ambiguity.

Going forward, a gap in the sequence is retired by default. If a number is skipped, note it here rather than leaving it silently reusable.

**Process note, added 6 Aug 2026:** F-043 (no admin UI existed for guest-slot scheduling at all) traces back to a real methodology gap flagged at the very start of this project — the BCC "Application Models" stage (wireframes/prototypes before code) was largely skipped, going from written business discovery straight to implementation kickoffs. A written requirement can read as "covered" while still being invisible in practice, because nobody had to visualize where it would actually live on a screen. F-043 was only found because the user personally tried to use the missing capability by hand, not because any review process caught it earlier. Lesson: for genuinely new capability (not a fix to something existing), a rough wireframe pass before the Technical Design kickoff is worth trying deliberately, specifically to surface "where does this actually appear on a screen" questions while they're still cheap to answer.

---

## Open

| ID | Found | Context | Description | Next step |
|---|---|---|---|---|
| F-002 | 29 Jul 2026 | Phase 8 | 7-day PWA dismissal-expiry logic never cleanly confirmed on real device | Deferred to Playwright coverage |
| F-003 | 29 Jul 2026 | Phase 8 | iOS install banner only confirmed via Chrome emulation, never a real device | Deferred until a real iPhone is available |
| F-004 | 30 Jul 2026 | Admin UI discussion | Production routing (Firebase Hosting rewrites, Cloud Run ingress) never formally decided | Parked, to be formalized before real deployment |
| F-005 | 30 Jul 2026 | Business discovery | Low-occupancy alert threshold mechanism built (admin-configurable %), but no default value chosen | Needs a business decision |
| F-014 | 31 Jul 2026 | Post-Phase 10 sweep | `payment.test.ts` exercises a test-only duplicate route, never the real Caddy-routed path | Low priority, not urgent |
| F-017 | 31 Jul 2026 | Admin Web Step 1 | All Admin Web screens in one large `main.tsx`, unlike guest PWA's per-screen convention | Worth splitting eventually, not urgent |
| F-024 | 31 Jul 2026 | Deployment/cost discussion | **Real OTP delivery still not integrated.** MSG91 was decided at original discovery, never actually built — every login across the entire project has used the dev-fallback fixed code. Firebase Phone Auth explored as an alternative (better SDK ergonomics, client-side reCAPTCHA + `signInWithPhoneNumber`, ID token verified server-side by Identity via Firebase Admin SDK) — but **requires the paid Blaze plan, ~$0.01-0.06/real SMS, not actually free**, comparable cost class to MSG91, not a cost-saving alternative. Firebase does offer genuinely free testing paths (Auth Emulator, registered test phone/code pairs with no real SMS) suitable for demo use. | Provider decision (MSG91 vs Firebase Phone Auth) not yet made. Not blocking the VM/demo deployment (dev-fallback or Firebase test numbers both work there at $0 cost) — needs deciding before any real customer goes live. |
| F-025 | 31 Jul 2026 | Deployment/cost discussion | **Real push (FCM) and real SMS notification delivery never verified.** The Notification service's dispatch mechanics (channel-policy routing, retry/backoff, dead-letter) are genuinely built and tested since Phase 5 — but no real device has ever received an actual push notification, and no real SMS notification (booking confirmations, refund notices, low-occupancy alerts) has ever been sent. | Needs a real-credential integration pass before real customer use, same shape as the Razorpay integration round — not urgent for demo. |
| F-026 | 2 Aug 2026 | Pre-GCP-deployment discussion | **No per-tenant payment routing exists — every tenant's guest/member payments currently flow into one shared Razorpay account, not the tenant's own bank account.** This has been true since Phase 4 and was never questioned until now — every payment test throughout this entire project (Phase 4, Phase 9, F-023's integration test, the real UPI payment confirmation) used one shared `RAZORPAY_KEY_ID`/`SECRET`. For a genuine multi-tenant SaaS, each tenant business needs their own payment destination — a serious legal/financial gap, not a UX issue. Real fix identified: Razorpay Route's Linked Accounts — each tenant completes their own KYC to become a Linked Account, payments include a `transfers` instruction routing funds directly to them. This requires: a new `Tenant.razorpayLinkedAccountId` field, a real tenant onboarding/KYC flow (genuinely new, non-trivial), updates to every payment-creation endpoint (`/payments/orders`, `/payment-links`, `/payment-links/negotiated`) to include transfer instructions, and a commission-split decision (likely 100% to tenant, given the decided pricing model is subscription-based, not per-transaction commission). **Webhook scope clarified 2 Aug 2026, confirmed via current Razorpay docs**: no distinct webhook URL or secret is needed per tenant — Route events deliver to the same single webhook URL/secret already in use, with the recipient tenant identified via `account_id` in the payload (Route/transfer events) or the existing `gatewayRef → PaymentIntent → Booking → tenantId` chain (regular payment events, unchanged). This meaningfully reduces the scope of the fix — the existing webhook handler needs new event-type handling and an `account_id` reconciliation lookup, not an architectural redesign. | **Does not block the current single-tenant GCP demo deployment** (still just `courtowner1`). **Is a real blocker before onboarding any second real paying tenant with real money involved.** Needs its own dedicated phase — Technical Design, tenant onboarding UX, real Razorpay Route integration testing — not a quick patch. |
| F-027 | 2 Aug 2026 | Payment UX discussion | **Desktop UPI QR flow has never been tested — only mobile UPI Intent has real verification.** Razorpay Standard Checkout natively handles both device contexts (app-intent buttons on mobile, a scannable QR code on desktop) without requiring separate integration code — but every real payment test in this project (the original Phase 10 test, the real-device manual walkthrough) was done on mobile only. Nobody has opened the checkout on an actual desktop browser to confirm the QR renders correctly, let alone scanned it with a phone to confirm a real payment completes that way. | Needs a real desktop-browser test: open checkout, confirm QR renders, scan with a phone's UPI app, confirm the payment completes and the booking confirms — same rigor as the original mobile UPI test. Not urgent for the current single-tenant demo, but worth closing before wider real usage. |
| F-033 | 3 Aug 2026 | GCP VM deployment | **Razorpay's webhook setup rejects a bare IP address as the URL — requires a real domain name, discovered empirically during deployment, not documented explicitly in Razorpay's own docs (which only state "must be public, not localhost").** This means real webhook delivery cannot be configured on the VM's current `http://136.114.99.17` address alone. Deployment proceeded with a placeholder `RAZORPAY_WEBHOOK_SECRET` rather than blocking on this — the app itself doesn't need a working webhook to run and be demoable, only the fully-automated payment-confirmation path does. **Update 4 Aug 2026**: real HTTPS now live via a free DuckDNS subdomain (`elitecourts.duckdns.org`) + Caddy's automatic Let's Encrypt cert. Webhook URL needs updating to the real domain in Razorpay's dashboard — expected to resolve this finding, not yet confirmed with a real webhook delivery test. | Update the webhook URL to `https://elitecourts.duckdns.org/api/payment/webhooks/razorpay`, then confirm with a real triggered webhook event that delivery actually succeeds. |
| F-034 | 4 Aug 2026 | GCP VM live testing, Payment flow | **Raw internal/technical error messages can reach the user-facing UI unfiltered** — surfaced during real payment testing (`crypto.randomUUID is not a function` shown directly, a raw Node runtime error, not a human-readable message). Same category of problem as F-029 (raw IDs leaking to users), but for error text instead of identifiers — likely present anywhere an unhandled backend exception's raw message gets passed straight through to the frontend rather than being caught and replaced with a generic, safe message. **Root cause of this specific instance now understood**: `crypto.randomUUID()` is called client-side in the browser (`CourtBooking.tsx`, `admin-web/main.tsx`) to generate idempotency keys — browsers restrict this API to secure contexts (HTTPS/localhost) only, and the VM was plain HTTP on a bare IP, so the browser correctly blocked it. Now resolved by the real HTTPS setup (F-033's update). The broader finding — that this raw error surfaced to the user unfiltered at all, regardless of root cause — remains open as its own UX gap. | Confirm the payment flow now completes successfully over HTTPS (root cause fixed). Separately, still needs the systematic error-message sanitization sweep described above — not urgent, its own focused pass. |
| F-036 | 4 Aug 2026 | GCP VM HTTPS setup | **`docker-compose.yml`'s `caddy` service only published port 80 to the host (`"80:80"`), never port 443** — meaning even after Caddy successfully obtained a valid Let's Encrypt certificate and was genuinely listening on 443 *inside* the container, nothing outside the container (not even `curl localhost` on the VM itself) could reach it. Confirmed via `docker compose ps` showing `443/tcp` with no host-mapping, versus `0.0.0.0:80->80/tcp` correctly mapped. Likely never caught earlier because HTTPS was never configured or tested until today. | Fixed — added `"443:443"` to the `caddy` service's `ports:` list, confirmed working with a real successful HTTPS page load. |
| F-037 | 4 Aug 2026 | Live GCP demo, pre-payment screen | **A fifth location showing a raw database ID directly to the user — the pre-payment/checkout screen displays the raw booking ID.** F-029's fix covered four specific locations (guest dashboard, member dashboard x2, admin assignments); this screen was never part of that sweep. Confirms F-034's broader point: this pattern (rendering a raw internal field instead of something designed for display) keeps recurring in new places, not fully closed by any single fix so far. | Fix this specific screen (don't show the raw booking ID — show something the guest actually needs, like a booking summary/reference in a friendlier format, or nothing at all if not needed). Given this is now the second time this exact pattern has recurred after a fix, worth considering whether a one-time full sweep across every screen (not just fix-as-found) is a better use of time than continuing to find these one at a time. |
| F-038 | 4 Aug 2026 | GCP VM live payment testing | **The actual root cause, confirmed after further investigation: `BookingPay.tsx` has a hardcoded fallback Razorpay key literal baked directly into the source code** — `import.meta.env.VITE_RAZORPAY_KEY_ID || import.meta.env.VITE_RAZORPAY_KEY || 'rzp_test_TJllXnaezST7MV'`. Since `docker-compose.yml`'s `caddy` service never passes `VITE_RAZORPAY_KEY_ID` as a build arg (a real, separate gap, still worth fixing), `import.meta.env.VITE_RAZORPAY_KEY_ID` has been `undefined` in every build so far, meaning the code has silently fallen through to this stale hardcoded key the entire time — regardless of any `.env` change, container recreate, or even a `--no-cache` rebuild. This is more serious than a build-caching bug: a payment credential silently falling back to an old literal value, with no visible error, is a real risk pattern worth being direct about — not just for this key, but as a coding pattern. | Two real fixes needed: (1) add `build.args: VITE_RAZORPAY_KEY_ID: ${RAZORPAY_KEY_ID}` to `docker-compose.yml`'s `caddy` service so the real value actually reaches the build. (2) Remove the hardcoded fallback string from `BookingPay.tsx` entirely — a missing payment credential should fail loudly/visibly, never silently substitute a stale literal value. Search for whether this same fallback pattern exists anywhere else in the codebase (e.g. `admin-web`'s equivalent negotiated-booking payment code) before considering this fully closed. |
| F-039 | 4 Aug 2026 | GCP VM live payment testing | **A direct-on-VM emergency fix (F-036's `"443:443"` port mapping, added via `nano` straight on the VM during live debugging) never made it back into the actual repo the agent works from.** When the agent was later asked to add the Razorpay build-args fix, it correctly edited its own local copy of `docker-compose.yml` — which still only had `"80:80"`, since it had never seen yesterday's live fix. That edit was transferred to the VM, silently overwriting and reverting the working `443:443` mapping. Same underlying pattern as the earlier findings-register drift incidents (F-020/F-021's ID collisions) — state existing in two places (VM-live vs. local-repo) with no sync step between them, causing a fix to silently regress. Not the agent's fault — it worked correctly from the information it actually had. | Going forward: any fix made directly on the VM during live debugging must be copied back into the local repo (or explicitly described to the agent) immediately, not left VM-only — same discipline already established for the findings register, now extended to infrastructure config files. Right now: pull the VM's current, correct `docker-compose.yml` (with both today's Razorpay args fix and the restored `443:443` mapping) back into the local repo, so this exact regression can't recur a third time. |
| F-040 | 4 Aug 2026 | GCP VM live testing | **Let's Encrypt's production rate limit hit: "too many certificates (5) already issued for this exact set of identifiers in the last 168h0m0s."** Root cause: `docker-compose.yml`'s `caddy` service never mounts a persistent volume for Caddy's `/data` directory (where obtained certificates are cached) — every container restart today (port-mapping fixes, the Razorpay rebuild, subsequent fixes) wiped the cache, forcing a genuinely new certificate request each time instead of reusing a valid cached one. Caddy automatically fell back to Let's Encrypt's staging CA once production was rate-limited, which real browsers will not trust — expect "connection not private" warnings, not "connection refused," from here until this resolves. | **Immediate**: add persistent named volumes for `/data` and `/config` to the `caddy` service in `docker-compose.yml`, so future restarts reuse the cached cert instead of requesting new ones — stops burning further rate-limit budget. **Timeline**: production rate limit resets per the error message (~24h from when it was hit) — real payment/HTTPS testing with a properly trusted certificate needs to wait for that reset; the staging cert can be used to test unrelated screens in the meantime if the browser warning is manually bypassed, but Razorpay's checkout widget likely won't work reliably against an untrusted cert regardless. |
| F-042 | 5 Aug 2026 | F-041 independent verification | **Minor, non-blocking UI inconsistency**: the Member Attendance dashboard's `WINDOW_NOT_FOUND` row displays in a different format (`Assigned start 23:55` / `Assigned window missing`) than the other four status rows (date-range + cutoff format). Flagged consistently across both rounds of independent verification — a real, if small, cross-screen consistency gap, same category as earlier UX findings (F-030). | Not urgent — worth a quick design pass to align this row's format with the others whenever convenient. |
| F-043 | 6 Aug 2026 | GCP VM live demo prep | **No Admin Web screen lets an admin create or schedule guest-bookable availability windows — a genuine operational gap, confirmed by direct testing.** `POST /resource-pools/:id/availability-windows` exists and works correctly (confirmed with a real successful call), but every window in this entire project so far has come from seed scripts, never a real UI action. An admin currently has no way to put new guest slots on the calendar day-to-day — either a one-off window or a recurring pattern — without a raw API call using the internal service key, which a real admin would never have access to. This is at least as fundamental an operational gap as F-035 originally was (no bookings visibility) — this is no bookings-*creation* capability at all for guest-facing slots. **Being addressed now, split into two phases**: Phase 1 (in progress) — recurring pattern + one-off overrides (closure and modified-hours), using lazy on-access generation, no scheduler. Phase 2 (deliberately deferred, tracked as F-044) — proactive expiry notifications, needs real scheduled-job infrastructure. | Phase 1 kickoff in progress. |
| F-044 | 6 Aug 2026 | F-043 Phase 2, deliberately deferred, scope expanded 6 Aug 2026 | **This platform has never had real scheduled/cron infrastructure — every "recurring" mechanism so far (the grace-period sweep, the low-occupancy alert) has only ever been triggered by an explicit API call, never a real timer.** Confirmed genuinely reusable, not a single-feature need — **three concrete use cases now identified**: (1) admin notification before a guest-slot pattern is about to expire (F-043's original trigger), (2) **guest reminder that their booked slot is approaching**, (3) **member reminder that their attendance-confirmation cutoff is approaching**, prompting them to open the app and confirm before the sweep releases their seat. All three share the identical underlying need: something firing proactively on a timer, with no user action required to trigger the check. | Needs its own dedicated Technical Design phase, planned after the current screen-review round (UI thread + friends' feedback + Claude Design polish pass) completes. Design the scheduler generically with all three known use cases in mind from the start, not narrowly for just one. **Phase A complete and committed (`9906b99`).** **Phase B scope constraint — `/bookings/sweep` must NOT be scheduled onto this mechanism until F-065 is resolved** (displayed vs. enforced member deadline disagree by 90 min); scheduling it would run the sweep on a real timer for the first time and convert that latent bug into live harm. F-066 (three disagreeing clocks; `Branch.timezone` never read) is a second live blocker — a 60-second scheduler crosses real midnight daily, which the hand-triggered sweep never did. **F-066 STATUS, 13 Aug 2026 — partially addressed, NOT cleared.** Stage 1 (`4665302`) makes the sweep derive weekday and date per branch from one clock, which makes the scheduler **safe to run against the current all-UTC deployment specifically**. It does not close F-066: every branch still reports `UTC` and nothing can change that, so the platform's real timezone problem is fully live and F-066 stays open pending [[F-088]]. **And F-065 remains a completely separate, independent blocker regardless of any F-066 progress.** Read "F-066 partially addressed" as exactly that — it is not "cleared to proceed". Phase B needs F-065 resolved *and* a deliberate decision about running against UTC-only branches. |

| F-046 | 6 Aug 2026 | Found during regression-suite consolidation's Playwright verification pass | **`test:e2e`'s specs cross-contaminate through a shared database when run together as one suite — proven, not assumed.** `f023-full-system.spec.ts` creates a user (`f023-admin-owner`, `+919999999999`) that collides with a different user `seed-test-data.ts` later tries to upsert with the same phone+tenant, causing a unique-constraint failure that takes down unrelated specs run afterward. Confirmed via isolated reproduction: specs pass individually, fail together, and the failure is unrelated to any code this project has touched recently — all three files involved (the two specs plus the seed script) predate the regression-suite work. **Further concrete evidence, 8 Aug 2026 (from the F-063 re-run):** two distinct mechanisms confirmed. (1) `seedF023`'s `afterAll` only calls `prisma.$disconnect()` — it performs no cleanup at all, so every F-023 fixture it seeds is left behind in the database after the run. (2) The sweep the spec triggers reaches well beyond its own fixtures — the run reported `lazyGeneratedCount: 3` and `lowOccupancyAlertsDispatched: 3`, i.e. it lazily generated member bookings and dispatched low-occupancy alerts for pools belonging to other specs' data. So contamination is bidirectional: the spec both leaves state behind and mutates state it does not own. | Needs its own investigation into proper test-data isolation between Playwright specs (e.g. per-spec unique tenant/phone namespacing, or a full DB reset between specs rather than relying on each spec's own cleanup). Not urgent — specs already had to be run individually in practice, this just makes the pre-existing limitation explicit and documented. **Analysis, not directly observed:** per-spec namespacing alone won't fix the sweep-reach half, since `POST /bookings/sweep` operates globally across all pools regardless of which spec triggered it — so any spec that calls it will touch other specs' data however their fixtures are named. (Reasoning from the endpoint's implementation, not a demonstrated failure.) |
| F-047 | 6 Aug 2026 | Found during regression-suite consolidation's Playwright verification pass | **`apps/guest-member-pwa`'s `test:e2e` script doesn't load `.env`, so it fails on a missing `DATABASE_URL` unless manually wrapped with `dotenv -e ../../.env`.** Every other test script in this project (the regression suites, the old phase test scripts) correctly loads `.env` itself. | Small, low-priority fix: add the same `dotenv -e` wrapper already used elsewhere to `guest-member-pwa`'s `test:e2e` script in `package.json`. |

| F-050 | 6 Aug 2026 | Found during F-045's fix work — a new exposure created by F-045's own fix, not independent | **`apiRequest` has no 401-refresh-retry, and this gap just became a genuinely reachable happy-path failure rather than a theoretical one** — because F-045 made auth mandatory on the booking/payment path where it previously wasn't enforced. Specific scenario: `BookingPay.tsx:127` verifies payment using a token captured in closure at an earlier point in time; a slow real UPI approval could let that token expire before `verify-payment` is called, causing a real payment (already captured by Razorpay) to fail server-side verification purely due to token staleness — a bad, confusing user experience and a real risk of Razorpay having captured money the system never confirms. | Needs a proper 401-refresh-and-retry mechanism in `apiRequest` (or equivalent) — not urgent enough to block F-045's already-shipped fix, but should be treated as a near-term follow-up given it's a direct consequence of that fix, not a coincidental separate bug. |
| F-051 | 7 Aug 2026 | Found while investigating F-048 — a distinct defect F-048 merely tripped over, more consequential than F-048 itself | **A field-casing bug (`window.starttime`/`endtime` vs. the real `startTime`/`endTime`) silently made two real checks into dead code.** Root cause: a raw `SELECT *` query returns Postgres's native lowercase column names, not Prisma's camelCase model field names — the two look identical in casual reading but are genuinely different objects. Two independent consequences: (1) the guest browse-ahead window check (`guestOpenWindowDays`, meant to cap browsing at ~7 days since F-043) silently never ran at all — the limit existed only on paper from the moment it was written, never actually enforced. (2) The `BlockedWindow` overlap check lost its time-bounds comparison entirely, meaning any blocked window blocked *every* slot in its pool, not just the overlapping ones — a real, currently-live over-blocking bug affecting genuine bookable capacity. **Worth flagging as a repeatable trap, not a one-off**: raw SQL meeting Prisma's casing convention anywhere else in this codebase carries the same risk — worth a quick audit for other raw-query call sites. | Fixed alongside F-048/F-049 (commit `d48977e`) — both consequences verified independently: a real overlapping `BlockedWindow` still correctly blocks, a real non-overlapping one on the same pool no longer wrongly blocks; a real request beyond the 7-day window is now genuinely rejected (confirmed as the intended, correct value, not just a side effect of the code fix). Worth a follow-up audit for other raw-SQL-meets-Prisma casing risks elsewhere in the codebase — not done as part of this fix, flagged for later. |
| F-028 | 1 Aug 2026 | UX/business-fit review, Guest journey | **Minimum group size is discoverable only after a failed submit, not before.** The pool's `minOccupancy: 2` rule is known to the screen (it renders correctly once tripped), but the guest can fill in a solo booking and tap "Hold & Proceed to Pay" before learning it's invalid. Confirmed via real screenshots and an actual API trace: `POST /slot-engine/bookings → 400 {"code":"INVALID_GROUP_SIZE"}`. | Make minimum occupancy visible on the slot/co-player step itself, and disable "Hold & Proceed to Pay" until group size is valid, rather than surfacing the rule only as a post-submit error. |
| F-031 | 1 Aug 2026 | UX/business-fit review, fresh empty-tenant walkthrough | **A brand-new, zero-data tenant gets no day-one setup guidance, and two screens (Low Occupancy, Negotiated) render their primary action as clickable even with nothing selected — inviting a click that can't succeed.** Note: narrower than first reported — Assignments and Resources correctly disable their equivalent actions in the same empty state, verified directly rather than assumed. Confirmed with a genuinely separate, unseeded tenant (`emptyowner1`), verified via the sidebar tenant name across all six screenshots, not a reused `courtowner1`. | Add a first-run setup prompt on Overview/Resources for zero-branch tenants, and disable or explain the primary action on Low Occupancy and Negotiated until required selections are made. |
| **F-023** | 31 Jul 2026 | **Restored — originally F-019, lost to an ID collision** | **No true cross-system integration test exists.** Every phase has real, verified testing within its own slice, but nothing has exercised a realistic connected sequence across all actors together (admin assigns member → unconfirmed → swept → alert → released → guest books and pays → refund override). | This is the active phase now — F-022 closed, unblocking it. |
| F-056 | 7 Aug 2026 | F-044 Phase A verification | `pnpm test:phase-a` currently exits non-zero on any runner without `ripgrep` installed (`spawnSync rg ENOENT`) — even when every substantive assertion has already passed. Environment dependency gap, not a code defect; "all assertions passed" and "the suite exits 0" are not currently the same claim. | Low priority — either bundle `rg` as a declared dependency, or replace the ripgrep-based check with a pure-Node equivalent so the suite doesn't depend on an external binary being present. |
| F-057 | 7 Aug 2026 | F-044 Phase A peer review | **Design finding, carried from F-044 Phase A's peer review.** The `ScheduledJobStore` port must not be exposed as a domain-facing handler API — the handler-facing boundary (not the raw store) is what makes the dispatch-policy fix actually hold. If a future handler bypasses the boundary and calls the store directly, the policy protection could be silently defeated. | Enforce this as an architectural constraint in Phase B and beyond — the raw store stays internal to the scheduler package, handlers only ever interact through the intended boundary API. |
| F-058 | 7 Aug 2026 | F-044 Phase A peer review | **Design finding, carried from F-044 Phase A's peer review.** The `?? 300` / `?? 3` default values are duplicated between the test assertion and the actual binding, with nothing forcing the two to agree — a future change to one without the other would silently create a mismatch between what's tested and what actually runs. | Extract to a single named constant, referenced by both the binding and the test, so the two can't drift apart. |
| F-059 | 7 Aug 2026 | F-044 Phase A scheduler runtime — **not** from the peer review; surfaced during register drafting, then independently confirmed against code 8 Aug 2026 | **Design finding.** `executeJob`'s catch block writes the handler's error to `lastRunError` in the database but issues no logger call — the only `logger.error` on that path belongs to `safelyComplete` and fires on completion-*update* failure, not on the handler error itself. The tick loop therefore swallows a handler's thrown error into the per-job failure/circuit-breaker bookkeeping, but nothing surfaces it to any real logging or alerting layer — a permanently broken handler would silently accumulate failures with no operator-visible signal until someone happens to query the circuit-breaker state directly. | Add real error surfacing (structured log line at minimum) whenever a handler throws, independent of the circuit-breaker's own internal accounting — an operator shouldn't need to know to look for this. |
| F-060 | 7 Aug 2026 | F-044 Phase A scheduler runtime — **not** from the peer review; surfaced during register drafting, then independently confirmed against code 8 Aug 2026 | **Design finding.** Job registration has no uniqueness check on `jobName`: `runtime.ts:60` builds `new Map(opts.jobs.map(job => [job.name, job]))`, so a duplicate name silently overwrites at startup with no error and no log line. **Worse than a simple overwrite** — `assertUsableSchedule` and `runDueJobsOnce` both iterate `opts.jobs` (the **array**), not the Map, so a duplicate registration would be claimed and executed **twice per tick**, while `runJobNow` would only ever reach the last one. The two code paths disagree about how many jobs exist. | Add a startup-time check that rejects a duplicate `jobName` registration, and make the array/Map divergence impossible by deriving both paths from one collection — a silent double-execution per tick is exactly what the claim mechanism exists to prevent. |
| F-062 | 7 Aug 2026 | F-044 Phase A design review | `low_occupancy_alert` notification reported as including a raw `poolId` — candidate for the same F-029-style raw-identifier leak pattern, not yet fully confirmed with a real screenshot/payload showing the leak. | Confirm with real evidence (the actual notification payload or a real screenshot) before treating as equivalent in confidence to F-029/F-037's already-proven instances of this pattern. |
| F-054 | 7 Aug 2026 | Found during F-044 (scheduler) design review | **`processQueue()` (Notification's retry mechanism) has exactly one production caller — `setImmediate` on a new send — meaning the 1/5/15-minute retry ladder for a *failed* notification only actually runs if an unrelated new notification happens to be sent afterward.** Real reliability gap, not a security issue. Directly relevant to F-044: new proactive reminders are about to be built on top of this same delivery layer. | Not fixed now — flagged as a known limitation for F-044's plan to explicitly acknowledge, not silently build on top of. Real fix (a genuine periodic queue-drain, ironically likely to reuse F-044's own new scheduler mechanism once it exists) can follow once F-044's core mechanism is proven. |
| F-063 | 8 Aug 2026 | Re-running F-023 against the current full stack | **The original F-023 integration test's assertion (`1 of 4 confirmed`, expected from `computePoolGuestOccupancy`) predates F-035/F-041's deliberate architectural split between guest occupancy and member attendance as separate metrics.** Re-run against the current system, the test correctly fails at this assertion (`services/slot-engine/src/index.ts` — `computePoolGuestOccupancy` filters `isMemberBooking: false`, so a confirmed *member* booking is excluded by design; observed `0 of 4 confirmed` against an expected `1 of 4`) — **not a regression, but confirmation the test itself has drifted from current, intentional design.** Real consequence: because the failure halts execution at `f023-full-system.spec.ts:269`, the entire back half of F-023's original promise — guest release, rebooking, payment, cancellation, refund override — **has not been re-verified against the current system** since F-035/F-041/F-043/F-044 all landed on top of it. F-022's sweep-protection mechanism was independently re-confirmed working in the same run (member booking stayed `CONFIRMED` with `memberAttendanceConfirmedAt` intact after a sweep run with `gracePeriodMinutes` shifted to 180). | Update the test's assertion to check member confirmation via `computeBranchMemberAttendance` (F-041's own mechanism) instead of the guest-only occupancy card, then re-run the full chain to genuinely re-verify it against current system state — **this has not yet happened.** |
| F-064 | 8 Aug 2026 | Found while re-running F-023 | **The guest PWA's Vite dev server binds to `::1` (IPv6) while `admin-web` binds to `127.0.0.1`, and Caddy proxies to IPv4 — causing a silent 502 on the guest app's root route** unless Vite is explicitly started with `--host 127.0.0.1`. Confirmed directly: `Get-NetTCPConnection` showed `5173 LISTENING on ::1` versus `5174 LISTENING on 127.0.0.1`, with `curl` to `127.0.0.1:5173` returning connection failure and Caddy's root route returning 502 while every `/api/*` route returned 200. No guard exists in the guest app's config against this, though the `Caddyfile`'s own comments warn about this exact class of problem. | Add the explicit `--host 127.0.0.1` binding to the guest PWA's dev script, matching `admin-web`'s existing configuration, so this can't silently recur. |
| F-065 | 8 Aug 2026 | F-044 Phase A design investigation | **Displayed vs. enforced member confirmation deadline disagree by 90 minutes.** The member dashboard and `POST /member/today-assignment/confirm` both compute `cutoffTime = window.startTime − gracePeriodMinutes` (default **T−30min**, `services/slot-engine/src/index.ts:677`, enforced at `index.ts:2317`). But sweep step 3 marks an unconfirmed member's booking `RELEASED_NO_SHOW` at the earlier `window.startTime − guestAccessCutoffMinutes` (default **T−120min**, `index.ts:2592-2609`), and confirm then rejects that status outright with 409 `CONFIRMATION_CUTOFF_PASSED` (`index.ts:2326-2331`) — so a member sees time remaining on screen while actually already locked out. **Never manifested in production because the sweep has never run on a real timer** — its only callers are regression tests. | Real product decision needed on which deadline is authoritative, before deciding whether the fix belongs in the UI, the sweep, or the meaning of `guestAccessCutoffMinutes` itself. **HARD BLOCKER on part of F-044's own future scope, recorded explicitly 9 Aug 2026: F-044 Phase B must not schedule `/bookings/sweep` onto the new scheduler until F-065 is resolved.** Doing so would immediately convert this latent bug into live harm — the sweep would begin running on a real timer for the first time, locking members out at T−120 while the UI still shows time remaining, and locking them out *before* the T−60 reminder window opens, so the member-confirmation reminder would never fire for exactly the members it targets. Noted prominently because this is the kind of cross-finding connection that is easy to lose once several findings are in flight at once. |
| F-066 | 8 Aug 2026 | F-044 Phase A design investigation | **Three separate clocks in one date-resolution path disagree during a real, recurring window.** `todayDateString` uses UTC (`now.toISOString()`, `services/slot-engine/src/index.ts:616-618`), `isoWeekday` uses local time (`now.getDay()`, `index.ts:620-622`), and the window lookup parses branch-local `startTime` as if it were UTC (`index.ts:658`) — despite `packages/database/prisma/schema.prisma:262` explicitly documenting it as *"HH:mm, branch local time"*. On an IST deployment, local weekday and UTC date disagree between 00:00–05:30 daily, causing a Monday session to resolve against Sunday's date. The sweep repeats the identical pattern separately (`index.ts:2563`/`2570`). **`Branch.timezone` exists specifically to prevent this** — declared at `schema.prisma:73`, collected at tenant setup and written by Tenant Management (`services/tenant-management/src/index.ts:218`, `:266`) — **but Slot Engine never reads it anywhere**; confirmed via a full-service grep returning nothing but a single unrelated comment. | Pick one canonical clock and make `Branch.timezone` the actual source of truth, wiring it into every date/weekday computation in this path. Currently dormant only because the sweep is hand-triggered — **F-044 Phase B's scheduler crosses real midnight every single day, converting this from theoretical to live and guaranteed the moment it ships.** |
| F-069 | 8 Aug 2026 | Found while investigating why a newly created walkthrough file did not appear as untracked | **Plain `git status` under-reports untracked files in this repo**, due to `core.fsmonitor=true` combined with `core.untrackedCache=true` — the filesystem-monitor daemon missed a newly created directory (`Implementation_Plan/F-044/`), which `git status` silently omitted while `git status -uall`, `git status --ignored`, and `git ls-files --others --directory` all correctly reported it. **Confirmed directly:** disabling fsmonitor for one command (`git -c core.fsmonitor=false status --short`) immediately showed the file. Direct consequence: **F-044's `findingsUntouched` guard relies on this exact same fallible command to prove scope compliance** (it shells `git status --short` plus `git diff --name-only`, and `git diff` covers only tracked files) — a new untracked file under `services/` could be silently missed by the same mechanism, meaning the guard can pass when it should fail. Same hollow-guard family as F-055. | Switch the guard (and any other scope-verification check in this project's review discipline) to `git status --short -uall` or `git ls-files --others --exclude-standard`. **Also needed:** re-run Phase A's original "Findings A-D untouched" verification using the corrected command, to confirm that specific proof wasn't itself affected by this blind spot — don't assume it's fine retroactively. |
| F-070 | 8 Aug 2026 | Found during the register reconciliation pass for F-044 Phase A | **`F-038`'s row contains an unescaped JS logical-OR operator (two consecutive pipe characters), which markdown reads as column separators — the row parses as 9 columns instead of 5, so its Description and Next-step content render in the wrong columns and its final column is dropped entirely.** The operator comes from a real code snippet quoted inside the finding's own text (the `VITE_RAZORPAY_KEY_ID` fallback chain). A fix attempt on 8 Aug 2026 replaced the operator with backslash-escaped pipes; that rendered as two *separate* pipe characters with a space between them, silently changing a logical-OR into something meaningless. The row was reverted byte-identical to HEAD rather than shipped with altered semantics. Note this entry deliberately describes the operator in words rather than reproducing it, to avoid the same defect in the row reporting it. | Fix using a representation that survives table parsing while preserving the operator exactly as written — HTML entities or a restructure that moves the snippet out of the table cell are both viable; naive backslash escaping is not, as demonstrated. **The constraint is that the content must not change, only its encoding.** |
| **F-071** | 9 Aug 2026 | Found during Tier 1 planning investigation; not fixed there | **The IDOR guard on three dual-path routes — `POST /bookings/:id/cancel` (`services/slot-engine/src/index.ts:2143`), `GET /bookings/:id` (`:2772`), `GET /bookings/:id/cancel-preview` (`:2850`) — fails in two opposite directions at once.** All three run an identical `isAdmin` expression that logically-ORs `roles.includes('owner')` with `roles.includes('branch_manager')`, then rejects with 403 unless the caller is the booking's owner or that `isAdmin` is true. (Described rather than quoted, per F-070 — reproducing the operator here would break this row's own table rendering.) **(a) Fail-closed half:** the bare string `'branch_manager'` never matches the real claim format, which is `branch_manager:<branchId>` (see `isAuthorizedForBranch`, `index.ts:170`, and `getInternalOrAdminAuth`, `:139`). A genuine branch manager who is not the booking's owner is therefore **wrongly denied** — a functional bug, not an exposure. **(b) Fail-open half, the serious one:** `roles.includes('owner')` *does* match, and these three handlers perform **no branch or tenant comparison whatsoever** — verified, there is no `tenantId` or `branchId` check anywhere in the guards. So any holder of the `owner` role can read, preview-refund, and **cancel any booking in any branch of any tenant**. Elsewhere in this file the same concern is handled properly by `requirePoolScope`/`isAuthorizedForBranch`, which re-read the owning branch from the database; these three routes bypass that entirely. Severity is driven by (b), not (a). | **Needs its own fix kickoff, same tier as F-053/F-061.** Replace the hand-rolled block in all three with the established `getInternalOrAdminAuth` + a real scope check against the booking's own `branchId`/`tenantId` read from the database — not the client. Confirm with a real cross-tenant attempt returning 403, and a real branch manager correctly *allowed* for their own branch (the fail-closed half must be fixed too, or the fix trades one bug for the other). |
| F-072 | 9 Aug 2026 | Found during Tier 1 planning investigation | `requireInternalOrAdmin` (`services/slot-engine/src/index.ts:163`) is dead code — a thin wrapper over `getInternalOrAdminAuth` with **zero call sites** in the file. Harmless today, but it reads as an available auth helper, so a future route could adopt it believing it provides scoping; it does not (it discards the returned `AdminAuthContext`, so no `requirePoolScope` or `isAuthorizedForBranch` follow-up is possible from it). | Low priority cleanup — delete it, or if it is meant to be the canonical "authenticate but don't scope" helper, give it a comment saying so explicitly. Worth folding into whichever pass next touches this file's auth helpers rather than a dedicated change. |
| F-073 | 9 Aug 2026 | Found during Track A's regression verification | **Test fixtures derive a booking window via `nextAlignedHour(N)` (a relative offset) while deriving the weekday via local `getDay()` — if the run crosses local midnight between those two computations, the window lands on a different calendar day than the assignment claims, producing spurious `WINDOW_NOT_FOUND` failures.** Confirmed as the shared root cause behind two independently-observed failures in the same session: the slot-engine `member-flow` section failed with `"state":"WINDOW_NOT_FOUND"` for an assignment whose `startTime` was `03:30`, at 23:33 local; and `apps/guest-member-pwa/tests/f023-full-system.spec.ts` (whose `seedF023` uses `nextAlignedHour(2)` with `daysOfWeek: isoWeekday()`) failed at a **different, earlier point** than it had ~90 minutes earlier in the same session — moving from the occupancy assertion to `#member-session-card` showing *"Today's recurring slot has not been opened on the court schedule yet."* as wall-clock time advanced. **Same underlying flaw as F-066, manifesting in test infrastructure rather than product code — reinforcing evidence for F-066's severity**, since the same clock inconsistency now demonstrably breaks both the product and the tests meant to verify it. Verified as pre-existing, not introduced by Track A: a stashed-changes baseline rebuild produced byte-identical failure output. | Fix fixture time-derivation to use one consistent clock throughout — ideally deriving both the window and the weekday from `Branch.timezone` once F-066 makes that the real source of truth — rather than mixing relative-offset and local-time computations. **Blocks a verification debt from Track A (see also the Tier 1 screenshot debt on F-061):** the two `f023` sweep call sites updated for F-053 are typecheck-verified only, because the spec now fails before reaching them. **Re-verify the full `f023` sweep chain once this and F-066 are addressed** — do not let "typecheck-only" become "verified" by omission. **CORRECTION, 13 Aug 2026 — SUPERSEDED, and the superseding matters more than the original note.** An earlier correction recorded here claimed this entry's documented trigger "is narrower than documented, and does not reproduce as written." **That claim was itself wrong.** It generalised from a single observation — the baseline suite passing under a deliberately mismatched process timezone (`TZ=Pacific/Midway`) at roughly 07:00 UTC — into a statement about the mechanism, when all it actually showed was that *that particular condition* does not trigger it. **Re-confirmed 13 Aug 2026 during F-065's implementation: the trigger is real and reproduces exactly as originally documented.** The mechanism is wall-clock, not timezone-mismatch: a fixed relative-offset window (`nextAlignedHour(4)`) lands on the FOLLOWING UTC day while the date-resolution logic still resolves "today", so `resolveTodayMemberAssignment` searches today's date plus the assignment's `startTime` and never finds it, and `/occupancy` counts only today's bookings. **Observed, not argued:** at 23:04 UTC the baseline suite — with all F-065 changes stashed — failed `Member self-confirm` with `WINDOW_NOT_FOUND` for an assignment whose `startTime` was `03:00`, and failed `F-048` with `confirmedSeats: 0`. Both had passed all day. That is a genuine two-hour daily failure window that had been open the whole time. **How it was caught is worth recording:** not by review, but by running into the identical defect while writing F-065's own new fixtures and recognising it rather than treating it as an unrelated failure. **Fixed in `967716a` via a `withinTodayUtc` helper** (`services/slot-engine/src/regression/_fixtures.ts`), which requests the offset a fixture wants and takes whatever remains of the UTC day, applied to `member-flow` (two sites) and `guest-booking`. Proven at 23:17 UTC — the hour that had been failing — with 28/28 slot-engine sections and 5/5 suites under `TZ=UTC`, `Asia/Kolkata` and `Pacific/Midway`. **Scope of the fix, stated precisely rather than as "the chain is now protected":** the *regression* fixtures are fixed and proven. `apps/guest-member-pwa/tests/f023-full-system.spec.ts` carried the same raw relative offset and was **also** pinned in the same commit, but it is **typecheck-verified only** — running it needs the full browser stack behind Caddy, which is exactly the `f023` re-verification this entry's next step already tracks. Do not read the fix as discharging that debt. |
| F-077 | 9 Aug 2026 | Discovered during F-061's production browser verification | **A deploy can silently serve a stale `migrate` image while genuinely rebuilding the other services, producing a half-applied schema indistinguishable from a complete one.** The `migrate` container reported `"No pending migrations to apply."` — reassuring, correct-sounding output — while running from a cached image built before three real migrations existed. The only tell was the count: **`9 migrations found` against 12 in the repo.** Production was therefore serving 12 Aug application code against a **3 Aug schema**, missing `20260806090000_availability_generation_phase_a`, `20260808070000_scheduled_jobs_f044_phase_a` and `20260809120000_booking_rule_unique_per_pool_f067`. **Auth-only smoke checks passed cleanly against this broken state** — unauthenticated `POST /bookings/sweep` and `POST /booking-rules` both correctly returned `401`, because neither touches the missing tables. The gap surfaced only when F-061's Path 1 hit a real `42P10` — *"there is no unique or exclusion constraint matching the ON CONFLICT specification"* — from F-067's upsert running against a database with no such constraint, leaving Admin Web's Resources screen returning 500 on save. Explicitly rebuilding `migrate` and re-running applied all three cleanly. **CORRECTED 9 Aug 2026 — the mechanism is broader than first recorded.** This entry originally concluded that F-043's Scheduling screen "has been broken in production since its code first shipped there, because its tables did not exist." A direct check against production disproved that: navigating to `/admin/scheduling` silently falls back to Overview and makes **zero** pattern/override API calls, because the **Scheduling nav item is absent from the deployed bundle entirely** — it exists in source at `apps/admin-web/src/main.tsx:428` and renders locally, but the Caddy static image carrying both Vite frontends was never rebuilt. So its code never shipped to production at all; users would have found no screen to click rather than a broken one. **The real finding is per-component staleness: a single deploy left three components at three different vintages — backend services current (F-053/F-061 verifiably live), the `migrate` image stale by three migrations, and the frontend bundle stale by at least F-043 Phase C — with no single check revealing it.** The `401` smoke tests only proved the services; the `42P10` only surfaced the schema; the missing nav item was only noticed by eye. Compounding cause: `caddy` is the heaviest image (both Vite builds on a 2 GB VM) and exhausts memory, so partial rebuilds that skip it are the path of least resistance and leave the frontend silently behind. | Force-rebuild the `migrate` image on every deploy (`docker compose build migrate`, or `--no-cache` if layer caching is suspect) rather than trusting Docker's cache for this image specifically — a stale `migrate` build is uniquely dangerous because it fails *silently* rather than loudly. Add a standing post-deploy check comparing the `_prisma_migrations` row count against the repo's migration folder count; **"no pending migrations" alone is not evidence of a complete deploy.** Worth also reconsidering deploy ordering so application code cannot ship ahead of its schema. **Per the correction, the verification must be per-component, not global** — a deploy is only complete when the backend services, the `migrate` image *and* the frontend bundle are all confirmed current, since each fails invisibly and independently. Give `caddy` enough memory to build rather than letting it be the step that gets skipped: resizing the VM to e2-medium for the build and back down afterwards is the proven route. |
| F-078 | 9 Aug 2026 | Found while diagnosing F-077 | **Corepack prompts interactively during the `migrate` image build** — `! Corepack is about to download https://registry.npmjs.org/pnpm/-/pnpm-11.21.0.tgz` / `? Do you want to continue? [Y/n]` — which would hang any fully scripted or automated deployment indefinitely, with no error and no timeout, just silence. Observed twice in one session; both times the build only proceeded because a human was watching the terminal. **Root cause identified while logging this: `packageManager` is not pinned in the root `package.json`**, so Corepack resolves a version at build time and asks before fetching it. Note `corepack enable` appears in three places — `deploy/gcp-vm/Dockerfile.node-service` lines 7 and 31 (build and runtime stages) and `deploy/gcp-vm/Dockerfile.caddy-static` line 7 — so any fix must cover all three, not just the one that happened to prompt. | Set `COREPACK_ENABLE_DOWNLOAD_PROMPT=0` in the Dockerfiles or build environment, **and** pin `packageManager` in the root `package.json` so Corepack fetches a known version rather than resolving one at build time. The env var alone suppresses the symptom; the pin removes the reason it appears. |
| F-076 | 9 Aug 2026 | Found during F-071's scoping investigation | **`GET /users/:id/roles` (`services/tenant-management/src/index.ts:409-413`) queries `roleAssignment` by `userId` with no tenant filter, and flattens `OWNER` to the bare string `'owner'` — discarding which tenant granted it.** Every issued JWT's `roles` claim comes from this endpoint (`identity-auth/src/index.ts:336, 439, 508`), so a token's role list is not tenant-filtered at source. Currently contained only because `User` is `@@unique([phone, tenantId])`, meaning a given `userId` belongs to exactly one tenant's user row — but **nothing constrains `RoleAssignment.userId` to that**, since it is a scalar with no foreign key, so the endpoint's correctness rests on an assumption the schema does not enforce. **This is not a missing convention — it is one endpoint inconsistent with the convention established immediately beneath it.** The platform's canonical scoping helper, `GET /users/:userId/branches/:branchId/check`, sits roughly twenty lines below in the same file (`:431-460`) and *does* filter role assignments by tenant, resolving the tenant from the resource first. So the correct pattern was already present and adjacent when this endpoint was written; it simply was not applied here. F-071's fix mitigates the practical exposure by comparing the token's own `tenantId` claim against the resource, but does not resolve this endpoint. | Add an explicit tenant filter, and preserve the granting tenant in the response rather than flattening `OWNER` to a bare role string — the flattening is what makes the tenant unrecoverable downstream. Worth auditing alongside F-071's fix rather than in isolation, since they concern the same trust chain. |
| F-075 | 9 Aug 2026 | Second occurrence this session; first seen during F-055's rework | **Inline `node -e` shell-escaping fails *silently* — no output, no error, non-zero nothing — rather than failing loudly.** Observed at least twice: once during F-055's scheduler work and twice more during Track B (a constraint red/green script and a data-cleanup script). Both Track B occurrences were caught only because database state was independently re-read rather than trusted after the unexpected silence — the cleanup command in particular appeared to have run, but a direct query showed the demo row still present and the modified value unrestored. **Had either partially executed, it could have left a dropped unique index or half-restored data with nothing to indicate it.** | Treat as a standing process rule, not merely a finding: **prefer a script file over inline `node -e` for anything touching shared or destructive state.** Separately worth determining whether this is a genuine environment/escaping bug fixable at the tooling level, or simply to be avoided by convention. Until then, the governing habit — already applied twice successfully — is: **when a command produces unexpected silence, verify the underlying state directly before assuming it was a no-op.** |
| F-079 | 12 Aug 2026 | Systems-thinking review | **No durable login activity audit trail exists — nothing anywhere persists "who logged in, when, from where" as a queryable record.** Verified against the schema: there is no audit, event or login-history model of any kind (`packages/database/prisma/schema.prisma` — the full model list contains none). The sharp mechanism is that **the one row that does carry login context is deliberately destroyed at exactly the moment worth auditing.** `OtpRequest` (`schema.prisma:387-399`) holds `phone`, `tenantId`, `ip`, `attempts` and `createdAt`, and is indexed on both `[phone, createdAt]` and `[ip, createdAt]` — the shape of an audit trail. But `services/identity-auth/src/index.ts:271` deletes it on **successful** verification (correctly, to prevent replay), and `:255` deletes it after three failed attempts, so a *successful* login and a *brute-forced* one both end with the record gone. Failed attempts that never reach three leave only an incremented `attempts` counter that vanishes with the row. `AuthSession` (`schema.prisma:367-376`) persists, but it is a credential store, not an audit trail: it records `userId`, `refreshToken`, `expiresAt` and `revoked` — **no IP, no user agent, no outcome, and no row at all for a login that failed.** The Google path (`index.ts:371`) is the same. No `request.log` call records any login outcome either (verified by grep), so even the transient container-log record is incidental rather than deliberate. | Design a real login-audit table — user, tenant, timestamp, outcome (success/failure and which failure), and the context already available at the point of decision (`ip` is already captured for rate limiting; user agent is not, and adding it is the only new capture required). Write it **before** the `OtpRequest` delete, not after, so the successful-login case is not the one that goes unrecorded. **Deliberately queued behind current work — not urgent standalone, but folds naturally into the broader post-Tier-1 systems review already planned.** |
| **F-080** | 12 Aug 2026 | Systems-thinking review | **The only rate limiting anywhere in this platform is OTP's, and it is hand-rolled rather than infrastructural — every other endpoint on a live public deployment has no volume limiting at all.** Confirmed by grep across `services`, `packages` and `apps`: the sole limiters are three checks inside `POST /auth/otp/request` (`services/identity-auth/src/index.ts:140` `COOLDOWN_ACTIVE`, `:157` `RATE_LIMIT_EXCEEDED` per phone, `:173` `IP_RATE_LIMIT_EXCEEDED` per IP), each implemented as a `prisma.otpRequest.count()` over a time window. **There is no rate-limit plugin or middleware in the tree at all** — no `@fastify/rate-limit` in any `package.json`, and no global `addHook`/`preHandler` performing volume checks in any of the five services. Two consequences follow. First, the mechanism does not generalise: it counts rows in a table that only OTP writes, so it cannot be pointed at another route without inventing new storage. Second, and more consequential, **every endpoint hardened across Tier 1 is now correctly authenticated but still unlimited** — F-053's `POST /bookings/sweep`, F-061's `POST /booking-rules`, F-071's three booking routes. Authentication establishes *who* may call; nothing establishes *how often*, so a single valid credential is enough to drive unbounded volume, including against the payment routes. This is infrastructure-level exposure on a public deployment with real payment flows behind it, not an application-logic gap. | **Flagged explicitly as a candidate for priority ahead of the standard queue, to be discussed rather than filed as routine backlog**, given the live exposure. The natural shape is a shared plugin registered once per service (the `packages/shared-middleware` boundary already exists for exactly this kind of cross-cutting concern), with per-route overrides for the expensive paths — not a second hand-rolled counter. Any fix must state which store backs the counters, since the OTP precedent's database-count approach does not survive being applied platform-wide. **Refined 15 Aug 2026 — "rate limiting" covers four distinct concerns, not one, and they do not share a priority for this platform.** (1) **True volumetric DDoS** — largely an infrastructure-layer concern (CDN / GCP-level), not urgent at current scale. (2) **Brute-force / enumeration** — the more relevant near-term risk *specifically here*, given F-029/F-037's proven history of booking IDs leaking into UI screens; an authenticated caller could still rapidly enumerate booking ids today with nothing slowing repeated requests. (3) **Cost / resource abuse** — endpoints triggering real external cost or heavy computation (payment calls, SMS once F-025 is real, availability generation, the sweep) could be hammered by one bad actor *or a buggy retry loop*, with no malicious intent required. (4) **Business-logic abuse** — repeatedly triggering a real side effect (refund creation, reminder spam) faster than intended. **Practical near-term priority: per-caller request throttling on authenticated endpoints, especially booking lookup-by-id and the payment/refund-triggering routes** — not full DDoS/infrastructure protection, which is lower priority at current scale. |
| F-081 | 12 Aug 2026 | Systems-thinking review | **Payment failures are not merely unreported — they are never recorded at all.** Logged as an observability/reporting gap on the premise that `PaymentIntent.status` includes `FAILED`, so the underlying data already exists. **That premise was checked and disproved before logging, which makes the finding more serious rather than less.** `PaymentIntent.status` is a free-form `String`, not an enum (`packages/database/prisma/schema.prisma:421`); the four intended values appear only in an inline comment (pending, captured, failed, expired — described rather than quoted, per F-070). Grepping every write across the repo shows **no code path anywhere ever assigns `failed` or `expired`.** The only values written are `pending` (`services/payment/src/index.ts:189, 812, 928`) and `captured` (`:343, :462, :546`). So a payment that genuinely fails leaves its intent sitting at `pending` — **indistinguishable from one still in flight, and from one the user simply abandoned.** The query "show me all failed payment attempts this week" cannot be answered by adding a report, because the rows to report on do not exist and the ones that do are mislabelled. Note the schema comment is what made the capability look present; the enum-as-comment is itself part of the mechanism. | **Two steps, in order, and the first is data capture rather than reporting.** (1) Write a real terminal status when a payment fails or expires — including the webhook `payment.failed` path, which is currently unhandled, and a rule for ageing out stale `pending` intents so abandonment is distinguishable from failure. Promoting `status` to a Prisma enum would make the absent values impossible to leave unwritten. (2) Only then is an admin-facing failure view meaningful. **Re-rank relative to F-079/F-080 in light of the correction** — this was queued as the lowest of the three on the assumption that the data existed; it does not. |
| F-087 | 13 Aug 2026 | Surfaced by F-066 Stage 1's alignment test, which could not be fixed without confronting it | **`POST /resource-pools/:id/availability-windows` accepts timezone-naive datetime strings and resolves them against the SERVER's clock.** `new Date("2026-07-31T22:16:00")` — no offset, no `Z` — is parsed by Node as process-local time, so the instant a caller creates depends on how the container happens to be configured rather than on anything the caller said. This was invisible while every layer was naive-local together: before F-066 Stage 1 the boundary check *also* read the server clock, so input and judgement agreed and the endpoint behaved consistently. **Stage 1 deliberately made the judgement branch-local while leaving the parsing server-local, so the two now come from different clocks.** Harmless in production today only because the server is UTC and every branch is `UTC`, making all three agree by coincidence. It becomes a real defect the moment [[F-088]] flips a branch to a real timezone: an admin entering `22:16` would have it stored as 22:16 UTC and then judged, displayed and aligned as 03:46 IST. Found because the F-010 regression test sent exactly such a naive string and was therefore silently asserting the test runner's own timezone — identical input produced `22:16` on an IST machine and `16:46` on a UTC one. | **Must be fixed as part of [[F-088]], before any branch is flipped — not after.** Either require an explicit offset and reject naive input with a 400 naming the reason, or interpret naive input on the branch's clock, which is what an admin typing a local time actually means. The second is friendlier and matches the schema's own "branch local time" convention, but it must be a stated decision rather than an accident. Audit the other datetime-accepting endpoints in the same pass: this one was found by chance, and nothing rules out siblings. |
| **F-088** | 13 Aug 2026 | Split from F-066 during Stage 1, 13 Aug 2026 — tracked separately so it cannot quietly vanish behind a "done for now" fix | **F-066 Stage 2 — the half that actually solves the platform's timezone problem.** Stage 1 (`4665302`) made Slot Engine's date logic correct and is provably a no-op in production, because **every branch reports `UTC` and nothing can change that.** Scope, all five parts required: (1) **make `Branch.timezone` genuinely settable** — it appears nowhere in `apps/admin-web`, `branchScheduleSchema` (`main.tsx:180-183`) carries only working hours, so Tenant Management's `timezone` fallback to `'UTC'` always wins and the column is unreachable from the product; (2) **a per-branch dry-run migration report before any write**, showing exactly which assignments and windows change meaning under the corrected interpretation — an assignment's `"10:00"` stops meaning 10:00 UTC and starts meaning 04:30 UTC, so existing windows stop matching; (3) **the actual flip** for real branches, informed by that report rather than assumed safe; (4) **`services/slot-engine/src/availabilityGeneration.ts`**, excluded from Stage 1 precisely because it is data-affecting — it is internally consistent UTC (`setUTCHours`/`getUTCDay`, helper literally named `atLocalUtcDate` at `:64`) and so has no self-mismatch, but it treats branch-local pattern times as UTC, meaning any change moves generated window timestamps; (5) **[[F-087]]'s naive-datetime parsing, which must land before the flip, not after.** **F-066 does not close until this lands.** Stage 1 fixed how dates are computed; the platform's actual timezone problem — every branch silently on UTC while operating in IST — remains fully live in production. | Full scope as described. **Not started.** Sequence (1) and (2) before (3); (4) and (5) are independent of the flip but must precede it. The production data audit F-066 Stage 1 could not perform — the local database is empty of assignments, windows and patterns, so it cannot answer the backward-compatibility question — is a **precondition** for the report in (2), and must be run against production with real output rather than inferred. |
| F-084 | 13 Aug 2026 | Carved out of F-083's fix verification — F-083 noted it, but as a trailing remark on a *resolved* entry it would have stopped being tracked | **`deploy:verify` cannot detect a TLS failure, and depending on the base URL it either misdiagnoses one or passes clean through one.** `scripts/verify-deployment.mjs` takes its base URL verbatim (`:27`) and concatenates each path onto it (`:54`); **there is no scheme, TLS or certificate assertion anywhere in the script** — the string `https` occurs exactly once, inside the usage-example text at `:23` (read from source, not from an observed run). Two consequences. **(a) With an `https://` base, a dead TLS listener fails all seven components at once** with connection errors, which is a real signal but reads as *"the whole deploy failed"* rather than *"TLS is misconfigured"* — the two demand completely different responses, and the observed F-083 red state was exactly this: `docker compose ps` reporting `running`, HTTP serving `200`, HTTPS refusing with `status=000`. **(b) With an `http://` base the script passes clean while HTTPS is entirely non-functional** — every component returns its correct SHA over plain HTTP and the script exits `0`. Nothing in the tool constrains which form is passed; the runbook's canonical sequence uses `https://`, but that is a convention in a document, not a property of the check. **This is the same shape as the flaw F-077 was written to fix** — a guard that reports success against a state it never actually examined — reappearing in the guard itself. | Assert the scheme and the transport, not just the payload: reject a non-`https` base for production (or require an explicit opt-out flag naming the reason, mirroring `ALLOW_UNVERIFIED_MIGRATE`), and add a distinct TLS check that reports *"TLS is down"* separately from *"components are stale"* so the two failures never present identically. Worth checking certificate expiry in the same pass, since the current production certificate expires 3 Nov 2026 and nothing anywhere watches that date. |
| F-082 | 12 Aug 2026 | Systems-thinking review | **Double-payment prevention is solid and repeatedly proven, but a blocked attempt is stopped silently — prevention and observability are different properties, and only the first exists.** Four distinct rejection paths were confirmed, none of which logs, records, or counts the blocked attempt: (1) and (2) the two atomic webhook idempotency gates, `services/payment/src/index.ts:426-438` and `:511-521`, which catch Prisma `P2002` on `webhookEvent.create` and `return { success: true, duplicated: true }` — note the `WebhookEvent` row that exists is from the **first** delivery, so nothing distinguishes an event delivered once from one replayed fifty times; (3) the payment-link gate at `:818-828`, where a `P2002` on the deterministic `gatewayRef` (derived by SHA-256 of the `Idempotency-Key` header, `:797-799`) is caught and the existing intent returned with `reused: true`, a flag sent to the caller and stored nowhere; (4) the refund gate at `:662-665`, which returns the existing `Refund` unchanged. Every one of these returns a **success** response, so the blocked attempt is invisible in status codes as well as in storage. The consequence is that a client retry loop, a gateway replaying aggressively, and a deliberate double-charge attempt are indistinguishable from normal traffic — the defence works and reports nothing, so its effectiveness cannot be observed and its failure would not announce itself either. | Record at the point of rejection in all four paths, not just the webhook pair — a duplicate-attempt counter or audit row carrying the key that collided, the intent it resolved to, and the caller. **Worth designing together with F-079's audit table rather than separately**, since both are "the system made a decision and kept no record of it" and a single audit mechanism can carry both. Related to F-080: a spike in blocked duplicates is exactly the signal a rate limiter would act on, and neither exists today. |
| F-092 | 14 Aug 2026 | Found during F-090's investigation | **`POST /bookings/:id/cancel` returns HTTP 500 instead of 401 on auth failure.** At `services/slot-engine/src/index.ts:2334-2337` the handler sets `reply.status(401)` but throws a bare `Error('Unauthorized')` without setting `(err as any).statusCode`, so the response envelope maps it to 500 — unlike the other auth helpers in the same file, which set `statusCode` explicitly. Observed live during F-090's control run: the unauthenticated cancel returned `HTTP 500 {"code":"INTERNAL_SERVER_ERROR","message":"Unauthorized"}`. **Auth still correctly rejects the request — only the reported status code misleads**, leaving a client unable to distinguish an auth failure from a server error. Same family as F-034 (internal/technical error detail reaching the caller unfiltered). | Low priority — set `(err as any).statusCode = 401` (and a `code` of `UNAUTHORIZED`) to match the intended response and the convention used by the file's other auth paths. Worth a quick scan for the same bare-`throw`-after-`reply.status` pattern elsewhere in the service while in there. |
| F-093 | 14 Aug 2026 | Open product question, split from F-090's guard decision so it is not settled by accident | **Open product question: should guest self-check-in carry a time-window safeguard, given that checking in irreversibly forfeits the guest's own refund path?** F-090's investigation established the mechanism: cancel accepts only `HELD`/`CONFIRMED` (`services/slot-engine/src/index.ts:2357-2360`) and no route transitions `CHECKED_IN` back, so **a guest who taps "I'm Here" permanently gives up their tiered refund** — the same consequence F-090 identified for an attacker, reachable here by the customer themselves, entirely by accident. Check-in is currently pure self-service: the only caller anywhere is the guest PWA (`apps/guest-member-pwa/src/components/BookingHistory.tsx:38-51`, button labelled "I'm Here" at `:245`), `apps/admin-web` contains no check-in code at all, and the E2E test names the behaviour "self check-in" (`apps/guest-member-pwa/tests/guest-booking.spec.ts:139`). Two distinct questions sit here and both are genuinely product decisions, not technical ones: (1) should self-check-in remain guest-initiated, or become a staff/venue-operated action — noting that **no admin check-in screen exists**, which is the F-043 pattern exactly, a capability that reads as covered in writing but is invisible on any screen; (2) if it stays guest-initiated, should the irreversibility be softened — a confirmation step, a reversal window, or allowing cancellation from `CHECKED_IN` — rather than a single tap silently forfeiting a refund. **F-090 was deliberately scoped to authentication only so this decision could not ride in on a security fix.** **UPDATE 16 Aug 2026 — question (1) is now decided; (2) remains open.** DECISION-017 (RE-012, `VALIDATION-017` RESOLVED) answers the actor question as **both**: "Booking customer and authorized branch staff may check in a booking", with six conditions — authenticated, tenant match, customer requires booking ownership, staff requires authorization for the booking branch, status `CONFIRMED`, and within the authoritative check-in window. So self-check-in stays, and staff check-in is added rather than replacing it; the guest UI does **not** need removal. **Question (2), reversibility, is untouched by that decision** — nothing in DECISION-017 addresses reversal, and `cancel` still refuses `CHECKED_IN` (`index.ts:2357-2360`), so a guest tapping "I'm Here" still irreversibly forfeits their tiered refund. That remains the open half of this finding. | **Remaining scope is (2) reversibility only.** Separately, DECISION-017's inclusion of staff **confirms** what this entry previously raised as conditional: `apps/admin-web` contains no check-in code at all, so staff check-in is now known missing-capability work in the F-043 family — a screen that does not exist — rather than a hypothetical. Worth tracking as its own item rather than inside this one, since it is a build, not a decision. Check-in timing enforcement stays F-094. |
| F-094 | 14 Aug 2026 | Split from F-090's scope decision — the technical half of the check-in precondition gap | **Check-in's timing rule is enforced only in the browser, so it disappears entirely once the server is the sole gate.** `isCheckInOpen()` (`apps/guest-member-pwa/src/components/BookingHistory.tsx:96-106`) implements the real rule — "same day and within 2 hours of `window.startTime`" — purely client-side; the server handler (`services/slot-engine/src/index.ts:2301-2321`) has no timing precondition whatsoever, only a status check. Confirms BR-092 and INVARIANT-FINDING-002 from the RE document set against real code. Practical consequence: any caller bypassing the PWA can check in at any time — days early, or long after the slot ended — and F-090's auth fix does not change this, since an authenticated booking owner still faces no timing check. **The client-side rule additionally carries the F-066 mixed-clock shape**: `now.toDateString()` compares process-local calendar days while `window.startTime` is a UTC instant, the same class of defect as F-066/F-086/F-073. | Enforce the timing precondition server-side. **Explicitly reuse `services/slot-engine/src/branchTime.ts`** (`branchDateString`, `branchIsoWeekday`, `branchMinutesOfDay`, `branchLocalToUtc`, `safeTimeZone`) — the helper set F-066 Stage 1 built for exactly this — rather than reimplementing date/weekday logic and risking a fresh mixed-clock bug. Note the dependency: a genuinely correct branch-local window needs F-088's Stage 2 work, since every branch still reports `UTC` and `Branch.timezone` is currently unreachable from the product; until then this is correct-but-dormant in the same way F-066 Stage 1 is. Sequence after F-090, and settle F-093 first if the answer changes who may check in at all. |
| F-096 | 15 Aug 2026 | Found during drawio extraction, CAP-010 screen mapping | **RE-003's "Executable Entry Evidence" field, when used to determine which UI screen a flow renders on, was wrong for 2 of 4 flows checked in CAP-010.** FLOW-045 and FLOW-046 both cite `apps/admin-web/src/main.tsx` - `OccupancyPage`; both actually render in `Overview()` — the guest-occupancy query at `main.tsx:618-620` and the member-attendance query at `main.tsx:623-625`, with `OccupancyPage()` not beginning until `main.tsx:1236`. The two flows that genuinely do live there, FLOW-047 (`main.tsx:1261`) and FLOW-048 (`main.tsx:1266`), cite it correctly — so the field is right half the time in the one capability checked. The `/occupancy` route is additionally labelled "Low Occupancy" in the nav (`main.tsx:430`), not a general occupancy page, so the name itself invites the wrong grouping. **Cross-corroborated independently**: the register's own F-041 describes member attendance as being on the Overview dashboard, matching the code and contradicting RE-003. The field appears reliable for *service endpoints* — every backend path checked during this extraction was correct — and unreliable specifically for *frontend component* attribution. | Treat this field as unverified for screen-mapping purposes anywhere in the RE set — spot-check against real source before trusting it for any other capability's diagram grouping, and do not assume CAP-010 was the only place this happened. Note the asymmetry when spot-checking: backend endpoint evidence held up, frontend component evidence did not. |
| F-095 | 14 Aug 2026 | Found during white-label verification | **Tenants with no logo set (`logo: null`) receive an empty icon array in their PWA manifest**, degrading the install-to-homescreen experience for that tenant specifically — no broken rendering, but no icon in the install prompt. Confirmed live against the real `club1` tenant: `GET /api/tenant/tenants/:id/manifest.json` returned `200` with `"icons": []` and `"theme_color": "#123456"`, and the rendered page carried zero `<img>` elements and zero broken images — the letter brand-mark fallback works correctly on screen. The gap is confined to the manifest icon set, which is what the PWA install prompt uses. `courtowner1` is unaffected (`logo: "/logo.png"`, loaded, `naturalWidth: 512`). Relevant to Phase 8's install work (F-002/F-003), which was verified only against a tenant that has a logo. | Low-to-medium priority — either a sensible default/fallback icon for logo-less tenants, or an explicit logo requirement at onboarding. Worth deciding before a second real tenant is onboarded, since the choice is an onboarding-flow question as much as a code one. |
| F-098 | 14 Aug 2026 | Found during F-096's spot-check | **FLOW-019 (create resource pool) has no admin UI anywhere in `apps/admin-web`.** `ResourcesPage` can only edit pools that already exist — it contains exactly three mutations: pool update (`main.tsx:757`), booking-rule update (`:766`) and branch schedule update (`:774`). There is no `POST /slot-engine/resource-pools` call anywhere in the app. **RE-003 cites this screen as executable entry evidence for pool creation, which is incorrect; no such path exists.** FLOW-021 (add resource to pool) has no admin caller either, though RE-003 does not claim one for it. This is **the F-043 pattern one level up**: F-043 found no UI to create availability windows; this is no UI to create the pools those windows depend on. Every resource pool in this project has come from seed scripts. **Directly relevant to imminent customer onboarding**: creating the customer's real pools/resources for the demo requires raw API calls with the internal service key — which works as a practical path today, but those same two endpoints are also unauthenticated (F-091), and there is no admin-facing way to do this at all going forward. Compounds F-031 (zero-data tenant gets no setup guidance). Note this is a distinct class from F-096: F-096 is "evidence names the wrong screen", this is "evidence names a screen that cannot perform the capability". **Real-world scale confirmed via customer evidence, 15 Aug 2026** (JBC's current WhatsApp-based operation, **17+ active batch groups** — distinct time-slot × court combinations): the eventual admin UI needs to support efficient creation and management of **many** pools, not just occasional one-at-a-time creation. Worth designing for batch/bulk creation from the start rather than a single-pool form that becomes tedious at real scale. This materially changes SCREEN-002's shape — a five-step wizard is right for onboarding a branch's first courts, but 17+ groups is a different job, and a wizard repeated seventeen times is the tedium this finding warns about. | Needs a real admin UI for pool/resource creation — not urgent enough to block the current demo (the internal-key workaround is available now), but a real gap for anything beyond one-off, developer-assisted onboarding. Worth scoping alongside F-031's first-run setup prompt, since both are the same zero-data-tenant journey. |
| F-100 | 15 Aug 2026 | Branch provisioning for JBC demo | **Both real JBC branches are provisioned with `timezone: UTC` rather than their truthful `Asia/Kolkata`, deliberately**, to avoid triggering F-087's naive-datetime parsing on real customer data during the demo period. Concretely: `POST /resource-pools/:id/availability-windows` parses `new Date(startTime)` against the **server** clock while judging boundaries against the **branch** clock (`getBranchTimeZone` is resolved immediately above it in the same handler), so a branch set to `Asia/Kolkata` would have a window entered as `07:00` stored as 07:00 UTC and read back as 12:30 IST. Leaving both branches at UTC keeps server, branch and input clocks agreeing by coincidence, exactly as every other branch on the platform does today. Note F-088's dry-run concern does **not** apply to these two — they are new branches with no pre-existing assignment or window data to reinterpret — so F-087 is the sole blocker here. **Accepted tradeoff for the demo window only, not a permanent state**: these two branches will report incorrect local operating hours to anyone reading raw timestamps. Tolerable for a two-week demo; not tolerable for an ongoing customer relationship. | **Must be resolved before this becomes a real production relationship.** Requires F-087 (naive-datetime parsing) and F-088 (Stage 2 timezone flip) to land first, then these two branches' `timezone` corrected to `Asia/Kolkata` as part of that work — explicitly as a step in it, not left indefinitely at the wrong value. Tenant `jbc`, branches `51f4f7e8-b14f-4ab0-bcfd-688d5c27d045` → `6c9c1e5e-d692-4957-960c-e5287f8c2d12` and `abdd89f9-4ae3-43f2-8d22-d78f2fa1d09e` (third provisioning; the first two sets were destroyed by F-101 before its guard existed). |
| F-103 | 15 Aug 2026 | Follow-up to F-101 | **The `test:regression` scripts still require manually setting `DATABASE_URL` to the disposable test database.** Without remembering to do so, F-101's guard correctly refuses and the suite aborts safely — so this is friction, not exposure. But the friction points the wrong way: the quickest way past a refusal is `ALLOW_DESTRUCTIVE_DB_RESET=true`, which is precisely the shortcut someone reaches for under time pressure, and precisely the class of shortcut that produced F-101 in the first place. A guard that makes destruction loud is real protection; a safe path that depends on memory is not the same thing as a safe default. | Wire the disposable database in as the actual default for these scripts rather than documenting it as the correct manual choice — e.g. `test:regression` targets `badminton_db_test` unless `DATABASE_URL` is explicitly overridden, with the migrate step pointed at it too. Same principle as F-101 itself: the safe path should require no memory to reach. Low priority, not urgent. |
| F-104 | 15 Aug 2026 | Captured from JBC's real WhatsApp attendance workflow | **Members have no way to signal "not attending" ahead of time.** In JBC's current WhatsApp process a member can reply "Out" in advance; the platform has no equivalent. A member's only states today are confirm, or stay silent and be swept past the cutoff — `resolveTodayMemberAssignment` exposes `canConfirm` and `POST /member/today-assignment/confirm` is the only member-initiated action. A proactive decline would let the seat release to guests **earlier than the cutoff**, rather than only at the last moment when the sweep runs, which is the difference between a fillable slot and a wasted one. **Explicitly scoped by the business as a binary response option added to the existing confirmation/reminder notification** — not a new booking state, not a new endpoint. That fits F-044's already-planned member-reminder use case (its case 3: "member reminder that their attendance-confirmation cutoff is approaching"), so it rides on infrastructure already designed rather than needing its own. | Not yet investigated. Pick up alongside or after F-044 Phase B, once the batch-scale work in F-098 is through. Note the scoping decision is what keeps this small — if it is later treated as a real booking state rather than a notification response, it stops being cheap and starts touching STATE-MODEL-BOOKING and the sweep's release semantics. |
| F-105 | 15 Aug 2026 | Captured from JBC's real operational pattern | **Members are moved between groups in JBC's real operation — different court, different time, or different branch, either as a genuine reassignment or a two-way swap — and it is unknown whether the platform supports this as a real operation.** `PATCH /member-group-assignments/:id` exists (FLOW-042, TRANSITION-MEMBER-ASSIGNMENT-002), but whether it can move an assignment across pools or branches, versus only edit its schedule in place, has not been checked. If a move today requires delete-and-recreate, that carries real side effects worth knowing before any design: lost attendance history, and potential re-triggering of assignment-start behaviour. **Unverified in both directions — recorded as an open question, not as a defect.** Connects to the CAP-008 zero-rule-coverage gap the architect review found: assignment and booking-rule configuration have no rule-level documentation in the RE set, so the intended semantics are not written down anywhere to check against. | Needs a real check of current behaviour before any design work — read `PATCH /member-group-assignments/:id` and confirm empirically whether a cross-pool or cross-branch move is accepted, and what happens to existing attendance records either way. Do not assume it works or that it does not. |
| F-106 | 15 Aug 2026 | Confirmed by DECISION-017 | **Staff-operated check-in is now a decided requirement with no UI to perform it.** DECISION-017 resolves VALIDATION-017 as "Booking customer **and authorized branch staff** may check in a booking" — so staff check-in is business-approved, not speculative. But `apps/admin-web` contains **no check-in code anywhere**: confirmed by search during F-090's investigation and again when DECISION-017 landed, not inferred. The only caller of `POST /bookings/:id/check-in` in the entire codebase is the guest PWA's "I'm Here" button (`BookingHistory.tsx:245`). The endpoint already accepts the staff path — F-090's guard is `requireInternalKey` \|\| `requireUserJwt` plus `requireBookingAccess`, and `requireBookingAccess` grants access to an `owner` or a `branch_manager:<branchId>` for the booking's branch — so the API is ready and only the screen is missing. **This is the F-043 pattern again**: a capability that reads as covered in writing while being invisible on every screen. Logged separately from F-093 deliberately — F-093's remaining scope is the reversibility *decision*, whereas this is a *build*. | Needs its own Screen Catalogue entry when picked up, same discipline as SCREEN-001/SCREEN-002. Worth scoping against F-094 at the same time: check-in timing is still enforced only client-side, so a staff screen built today would inherit no timing rule at all. |
| **F-107** | 15 Aug 2026 | External validation via Flutter reference screens | **Independent external evidence for F-065.** A competitor-reference member screen, built by a customer-side sales SME with no knowledge of this codebase's internals, shows the member attendance cutoff as **"Cutoff: 5:00 PM (2hrs before)"** against a **7:00 PM** session — that is **T−120**, which matches `guestAccessCutoffMinutes`, the value the sweep **actually enforces** when it marks an unconfirmed member `RELEASED_NO_SHOW` (`services/slot-engine/src/index.ts:2592-2609`). It does **not** match `gracePeriodMinutes` (T−30, `:677`, enforced `:2317`), which is what the product currently **displays** to members and what F-065 records as the disagreeing half. The screen also models attendance as an **IN/OUT toggle** rather than a one-way confirm, consistent with F-104. **Why this is worth logging separately rather than as a note on F-065**: every input to F-065 so far has been internal code reading. This is the first evidence from outside the codebase, produced by someone reasoning about what the market expects rather than about what the implementation does, and it lands on the enforced value. | **Fold directly into F-065's resolution** as evidence for which field should become authoritative — external market signal, not an internal engineering preference. Note the downstream dependency: SCREEN-002's Step 4 copy is explicitly gated on F-065, and is currently written for a single deadline governed by `gracePeriodMinutes`; if F-065 resolves toward T−120 instead, that copy needs revisiting before SCREEN-002 is built. |
| F-108 | 15 Aug 2026 | Flutter reference screens evaluation | **The payment service exposes no readable data at all — a member cannot read any payment or subscription information today.** Every route in `services/payment/src/index.ts` is a `POST` (intents, create-order, verify-payment, subscriptions, both webhooks, refunds, refunds/override, payment-links, simulate-capture); the only `GET` is `/health`. There is no transaction history endpoint, no amount-due endpoint, no subscription detail endpoint. **The data itself exists and is complete enough to serve a history view**: `PaymentIntent` rows carry `purpose` (including `subscription_billing`), `referenceId` (the subscription or booking id), `amount`, `status` and `createdAt`. It is purely unreadable, not missing. Partial exception worth recording: `GET /member/today-assignment` already returns `subscriptionStatus` when a subscription is active, so a member can see *whether* they are active — just not the amount, the history, or anything else. | Needs a real read-only endpoint. This is the genuinely designable half of the member-payment screen — a transaction list over data that already exists — as distinct from F-109, which needs a business decision first. Scope it with the tenant/ownership rules DECISION-015 settles, since payment reads are exactly the boundary that finding is about. |
| F-109 | 15 Aug 2026 | Flutter reference screens evaluation | **`Subscription` cannot express when money is due.** The model is `id, userId, tenantId, mandateId, amount, frequency, status` — with **no due date, no billing-period anchor, no paid-through marker, and no next-payment date**. A member-facing "Amount Due ₹1,200 / Due 15/10 / Overdue" cannot be computed or reliably derived: a date could be inferred from `createdAt + frequency`, but nothing records which periods have actually been settled, so "overdue" has no truth source. Compounding this, the subscription model is **mandate/AutoPay-based** — `mandateId` plus `/webhooks/razorpay/autopay` driving activation and suspension — so money is designed to be pulled automatically. The reference screen's member-initiated "Pay Now" for an overdue month is a **different payment model**, not a button over the existing one. | **A real business/billing-model decision is needed before this becomes a screen.** Unlike F-108 this is not a data-exposure gap — the data does not exist. Decide first whether billing is autopay-only with suspension on failure (what is built), or autopay with a manual settlement path (what the screen implies), because that choice determines the fields rather than the reverse. |
| F-110 | 15 Aug 2026 | Flutter reference screens evaluation | **No persistent "regular playmates" group exists, and no member can see another member's attendance status.** The reference member screen shows a roster of five named playmates each carrying a live **IN / OUT / PENDING** state — effectively JBC's WhatsApp group digitised, which is the workflow the platform is meant to replace. Today `BookingPlayer` records co-players **on a single booking** and `PendingInvite` handles invitation resolution, but neither is a durable roster, and nothing anywhere exposes one member's attendance to another. **This is the one item in the whole Flutter evaluation with a people-facing consequence rather than a data-modeling one**: it would make each member's attendance visible to other members by default, which is a decision about what people may see about each other, not a schema question. Worth noting the same screen is arguably the most competitively distinctive of the five. | **Needs an explicit privacy-scope decision before any design work** — who may see whose attendance, whether it is opt-in, and whether a member can leave a group's visibility without leaving the group. Do not begin a Screen Catalogue entry for this until that decision exists, since the answer determines whether the feature is a roster, a per-session opt-in, or nothing. |
| F-114 | 15 Aug 2026 | Business discussion, MVP scoping | **SME wants an MVP launch option to disable minimum-occupancy enforcement for guest bookings, toggleable per pool rather than a permanent removal.** Confirmed a real, currently-enforced rule, not a missing feature: the single enforcement site is `services/slot-engine/src/index.ts:2081` inside the `POST /bookings` transaction — `if (groupSize < pool.minOccupancy)` → 400 `INVALID_GROUP_SIZE` (`:2082-2085`), matching the live API trace already recorded in F-028. It is the **only** enforcement of `minOccupancy` in the service; the other references (`:974`, `:1013-1022`) are the admin create/update paths. **Scope matches the SME's intent exactly with no member-path leakage**: `POST /bookings` is the guest path — per F-048's note at `:2094-2099`, real member bookings are created server-side by `ensureTodayMemberBooking`, and `minOccupancy` appears nowhere in that function — so gating `:2081` affects guest bookings only. **One correction to the proposed placement, for the reviewer to settle**: the boolean precedent `prepaymentRequired` sits on `BookingRule` (`schema.prisma:276`), but the field being gated, `minOccupancy`, sits on `ResourcePool` (`schema.prisma:123`). Both are effectively per-pool — F-067 made `BookingRule` unique per pool — so either placement satisfies "per pool", but they are different tables and the choice should be explicit rather than assumed. **Following the stated `BookingRule` precedent costs no extra query**: the rule is already loaded inside the same transaction at `:2101`, twenty lines below the check, so it needs only the rule fetch moved above the group-size check. **Known consequence to handle**: `BranchDashboard.tsx:145` advertises "Min Players" whenever `pool.minOccupancy > 1`, so with enforcement off the PWA would still display a minimum that no longer binds. | Small, clean addition once the table is chosen — no separate feature-flag infrastructure. **Couple this with F-028**, whose remediation is to surface the minimum pre-submit and disable the CTA until group size is valid; that UI must read the same toggle, or disabling enforcement would leave guests blocked by a client-side gate the server no longer applies. |
| F-116 | 15 Aug 2026 | JBC demo provisioning | **JBC's admin owner will be provisioned with a placeholder phone number for the demo, not their real intended admin contact** — a temporary state to be unwound, not a resolved setup. **Superseded state — the placeholder is now live.** At logging JBC had zero `RoleAssignment` rows; the owner has since been provisioned as part of F-115's close: user `006ff1bd-a312-4992-bb51-b79711a91d4f`, phone **`+917777777777`**, `MEMBER`, holding `OWNER` tenant-wide (`branchId` null) — one row, confirmed by a real admin-web login showing both JBC branches. The number follows the existing obviously-fake local convention (`courtowner1` = `+919999999999`) and is **not** JBC's real admin contact. Recorded here as a commitment attached to the provisioning step about to happen, so the placeholder cannot quietly become permanent once it does. Working precedent for the shape is `courtowner1` — `OWNER`, tenant-wide, `branchId` null. | **A dated commitment, not a backlog item — same framing as F-100.** Capture JBC's real intended admin contact number during or after the demo conversation, then re-provision the owner role with it. Must be swapped before actual customer handoff. **F-115 is now fixed, so the swap is a one-liner**: re-running the same assignment with the real number updates the existing row in place instead of inserting a second owner — verified by running it twice during provisioning and getting the same row id both times. |
| F-118 | 15 Aug 2026 | Business requirement | **New restricted-access admin tier: full operational access except payment-history visibility and pricing configuration.** Confirmed a genuinely new permission dimension — every authorization check in every service accepts only `owner` or `branch_manager:<branchId>` (call sites listed in F-117), so the model scopes by branch and has no feature or capability axis at all. **Two findings that make this larger than a per-endpoint check.** (1) **There is no payment-history endpoint to gate.** The payment service exposes exactly one GET route, `/health` (`payment/src/index.ts:53`); every other route is a POST that performs an action. Payment and refund data reaches admin through **embedded fields on shared payloads** — `price` and `refundAmount` on booking objects (`admin-web/src/main.tsx:122-123`), `RefundPreview` (`:127-131`), and the `/refunds` admin route (`:432`). Enforcement is therefore **field-level filtering on responses other roles legitimately need**, not route-level gating — which is why no single central switch can express it. (2) **`FRONT_DESK` already exists and is entirely unimplemented**: it appears only in `schema.prisma:43`, the Phase 3 migration, and the Prisma-generated client typings that mechanically mirror the enum (`packages/database/src/generated/client/index.d.ts:168`) — **no hand-written code references it at all**, and no auth check, route or UI consumes it. Its original design intent — "front_desk (check-in/override only)" — is precisely the capability-scoped axis this requirement needs. A user assigned FRONT_DESK today cannot even load admin-web, since `roleCanAdmin` (`main.tsx:220`) accepts only owner and branch_manager. Pricing configuration additionally spans ~16 slot-engine routes carrying `defaultRate`/`pricingMode` (pool create `:921`, pool patch `:986`, availability windows `:1094`, patterns `:1219`/`:1235`, overrides `:1388`/`:1426`) plus the admin-web forms at `:171-172`, `:191-192`, `:204-205`, `:742-743`, `:801`. | **Needs its own investigation and design pass before implementation — same rigor as VALIDATION-015's tenant/ownership work**, since this is a comparable authorization-architecture decision on a new axis. **Settle first**: whether this tier implements or subsumes `FRONT_DESK` rather than adding a parallel mechanism beside an unused enum value, and whether the permission model is role-based or capability-flag-based. **Then** the response-filtering question, which is the real cost driver. |
| F-119 | 15 Aug 2026 | Found during member-creation-gap investigation | **HIGH SEVERITY — `POST /users/resolve-invite` (`services/identity-auth/src/index.ts:571`) has zero authentication.** Verified three ways: no `jwtVerify` in the handler, no internal-key check, and **no global `addHook`/`preHandler` anywhere in the service**, so nothing guards it at the framework level either. The handler goes straight from `request.body` to `prisma.pendingInvite.upsert` on `(phone, tenantId)` (`:576-580`). Any unauthenticated caller can therefore write `PendingInvite` rows for arbitrary phone/tenant pairs. Those rows are **silently consumed at signup** (`:296-320`): the first person to complete OTP on that phone has the invite deleted and `POST /bookings/resolve-invites` dispatched to slot-engine under the internal service key, linking prior co-player booking history to that new account. Same severity class as F-053, F-045 and F-090 — all zero-authentication routes — **in a service F-091's sweep never covered**, since that pass was scoped to slot-engine. | **Needs its own dedicated fix kickoff**, same priority tier and same rigor as the earlier chain: real live-fire RED proof against `badminton_db_test` showing the unauthenticated write landing and a row read back, contrasted against a protected sibling, before any guard is written. Note the correct guard shape is not obvious — this endpoint has no current caller in admin-web or the PWA (verified: admin-web has seven mutations, none to identity), so whether it should be internal-key-only, admin-JWT, or removed outright is part of the kickoff, not a foregone conclusion. |
| F-120 | 15 Aug 2026 | Member-creation-gap investigation | **No admin-reachable path exists to create a member at all — two missing layers, not one.** (1) **Promotion is structurally closed to admins**: `PATCH /users/:id/type` (`identity-auth/src/index.ts:587`) rejects anything that is not `Bearer ${INTERNAL_SERVICE_KEY}` with 401 and has **no JWT path whatsoever**; the comment states the intent — *"Prevents clients from spoofing user types."* An admin browser session holds a tenant-scoped JWT and can never call it. Tonight's JBC owner provisioning succeeded only because the script sent the internal service key directly. (2) **User creation has no admin endpoint to expose**: the only production `prisma.user.create` in the entire codebase is `identity-auth/src/index.ts:285`, inside `POST /auth/otp/verify` — every other create is a regression fixture writing to the database directly. A User row can therefore only come into existence when **the person themselves completes an OTP on their own phone**. **UI confirmation**: `AssignmentsPage` (`admin-web/src/main.tsx:1121`) selects members solely through `UserLookup` (`:558`), which is one phone field plus `GET /identity/users/lookup`, and calls `onResolved(null)` on failure (`:571`) with no not-found branch; admin-web contains seven mutations total and none touches users. **`PendingInvite` is not a half-built version of this** — it links prior co-player booking history and does **not** promote to MEMBER; the user is still created `GUEST` at `:285`. **Distinct from F-105** (moving an existing member between groups/times/branches) and **larger than F-098**, where the endpoints existed and were admin-reachable and only the UI was missing. | **Real design/architecture work before any Screen Catalogue pass.** The deliberate anti-spoofing protection means a direct admin create-and-promote endpoint would reverse an explicit security decision, so the likely shape is an **internal-key-brokered flow — an admin-initiated invite the person completes themselves** — rather than a direct create. Settle that before design. Batch scale is **unevidenced**: the database holds zero `MemberGroupAssignment` rows and JBC has 1 MEMBER (tonight's owner) / 2 GUESTs, so F-098's 17+ batch context is a plausible parallel but needs SME confirmation rather than assumption. |
| F-121 | 15 Aug 2026 | Member-creation-gap investigation | **`POST /member-group-assignments` (`services/slot-engine/src/index.ts:2731`) never validates `userType`.** It authenticates and pool-scopes correctly (`getInternalOrAdminAuth` then `requirePoolScope`, `:2732`/`:2744`) but then passes the caller-supplied `userId` straight into `memberGroupAssignment.create` (`:2747-2748`) with no membership check. A **GUEST can therefore be assigned to a member slot and the call succeeds**. The resulting state is silently half-onboarded and invisible: the assignment renders in the admin table, while `requireMemberJwt` (`:2601`) correctly blocks `GET /member/today-assignment` and `POST /member/today-assignment/confirm` with 403 `MEMBER_REQUIRED`, and the PWA hides its member surface entirely (`guest-member-pwa/src/main.tsx:170`). **No error surfaces anywhere** — the admin sees a successful assignment, the person sees nothing. Reachable today, because `GET /users/lookup` returns GUESTs (F-122) and the screen accepts whatever it returns. | **Should be part of F-120's eventual fix — add a real `userType` check to this endpoint regardless of how member creation itself is solved.** Worth deciding the failure mode explicitly: reject with a typed error, or auto-promote, the latter being unavailable to an admin JWT for the same reason as F-120. |
| F-122 | 15 Aug 2026 | Member-creation-gap investigation | **`GET /users/lookup` (`identity-auth/src/index.ts:46`) does not filter by `userType`** despite the admin field being labelled "Member phone" (`admin-web/src/main.tsx:576`). It selects `{ id, phone, userType }` (`:96`) for any user matching `(phone, tenantId)` and returns GUESTs identically to members; the UI then renders `Using {phone} ({userType})` (`main.tsx:581`), so the type is displayed but nothing acts on it. This is the reachability path that makes F-121 exploitable in ordinary use rather than only via a crafted request. Authorization itself is sound — admin JWT required, tenant-scoped, `owner`/`branch_manager:*` enforced (`:75-83`), phone normalized against `^\+91[6-9]\d{9}$` (`:86`), 404 `USER_NOT_FOUND` when absent. | Minor, and **likely folds into F-120's fix**. Note it may be correct to leave the endpoint unfiltered and fix the *label* plus the consuming screen instead, since an admin legitimately needs to look up guests elsewhere in the app — decide alongside F-120 rather than narrowing the endpoint by default. |
| F-111 | 15 Aug 2026 | Business discussion | **New notification: warn a guest as they approach a drop in their cancellation refund percentage**, so they can decide whether to cancel while the rate is still favourable. Triggered by the tier boundaries in `BookingRule.cancellationPolicyJson` — the default policy is `{type:'tiered', tiers:[{min_hours_before_slot:24, refund_percent:100},{6,50},{0,0}]}` — so the natural trigger points for that default are approaching T−24h (100% → 50%) and approaching T−6h (50% → 0%). **Genuinely new trigger logic**: every existing and planned notification fires relative to slot timing, whereas this one fires relative to a *policy* boundary, which is per-pool configurable and can have any number of tiers. Note the policy JSON is currently unvalidated server-side (any JSON is accepted), so trigger logic must tolerate a malformed or empty `tiers` array rather than assume the default shape. **Real design questions before build**: which boundary triggers it — every tier, or only the first drop; how far ahead of the boundary it fires; and whether that lead time is admin-configurable or a sensible fixed default. | **Natural home is F-044's scheduler** — a fourth genuine use case for the same proven mechanism (F-044 currently records three). Needs its own design pass. Not urgent, queued behind current work. |
| F-112 | 15 Aug 2026 | Business discussion | **New notification: send the assigned court number to a guest 120 minutes before slot start.** Standard fixed timing, deliberately not admin-configurable initially. **Depends on F-113 (the court-assignment capability itself) existing first** — a POOLED booking has no court to send until one is assigned. **Real edge case to decide**: what happens if no court has been assigned by the 120-minute mark — the notification fires without a court number, or it does not fire until a court is assigned. The second is safer but means a guest may receive nothing at all; the first means sending a reminder that answers the wrong question. | **Natural home is F-044's scheduler**, alongside F-111. Sequence after F-113, since the notification is meaningless without the capability. |
| F-113 | 15 Aug 2026 | Business discussion | **New admin capability: assign a specific court (`Resource`) to an existing POOLED booking after the fact.** This does not happen anywhere today — verified: `Booking.resourceId` is written **only at creation**, and only for `FIXED_INSTANCE` (`services/slot-engine/src/index.ts:2146` for the explicit path, `:2183` for the conditional `pool.allocationMode === FIXED_INSTANCE ? resource : null`). There is no `booking.update` writing `resourceId` anywhere in the codebase, so a POOLED booking's `resourceId` stays `null` for its entire life. JBC is provisioned POOLED on both branches, so every one of its bookings is in exactly this state. **Requires three pieces**: (1) an admin view listing today's bookings for a pool with their assignment status, (2) the assign action and endpoint itself, (3) validation that the chosen court is not already committed to a conflicting booking at that time — note the existing FIXED_INSTANCE conflict check (`:2120-2121`) reasons over `resourceId` at *creation*, so post-hoc assignment needs its own equivalent. **Separable from F-112 and worth building independently**: this capability has value with no notification at all — an admin can simply tell a guest their court in person or on arrival — whereas F-112 cannot exist without it. | **Needs its own Screen Catalogue design pass** — a genuinely new admin screen, not a variant of anything existing. Worth scoping alongside F-098's batch-scale context, since an operator running 17+ groups assigns courts at that same scale, and a one-booking-at-a-time assign screen would hit the same tedium F-098 warns about. |


## Resolved

| ID | Found | Resolved | Context | Description | Resolution |
|---|---|---|---|---|---|
| **F-085** | 13 Aug 2026 | 13 Aug 2026 | Found while running F-066 Stage 1's verification, when a fixture edit provably had no effect | **`services/slot-engine/tsconfig.json` excluded its regression sources from the build — the only one of five services to do so — so `pnpm -r run build` never compiled slot-engine's regression test code, and the suite ran from a stale `dist/regression` artifact of unknown vintage.** Proven directly rather than reasoned about: deleting `dist` and running the documented build left `dist/regression` **absent**, while `identity-auth`, `tenant-management`, `payment` and `notification` all regenerated theirs. The artifact could therefore only have come from a build predating the exclusion, so every "slot-engine regression PASS" was reporting on test code of unknown age. **Precisely the F-055 hollow-test class**, with the same signature: the suite looked like it was verifying current behaviour and was not. The blast radius is bounded but real — the *service under test* was always current, because the harness spawns it from `dist/index.js`, which the build does produce. What silently had no effect were changes to **fixtures, sections and assertions**. Found only because an F-066 fixture change appeared to do nothing and the compiled output was checked, rather than the symptom being explained away. | **Fixed by removing the exclusion**, aligning slot-engine with the other four services. Confirmed by deleting `dist` and rebuilding: `dist/regression` is now produced by the documented build and carries the current source (`setUTCMinutes` present in the compiled fixture). Whole-repo typecheck stays clean with the regression sources now in the compile, so the exclusion was not hiding type errors. **Standing lesson: when an edit appears to have no effect, verify the build output before explaining the symptom** — the same habit that caught [[F-075]]. |
| **F-086** | 13 Aug 2026 | 13 Aug 2026 | Found during F-066 Stage 1's implementation, not during its investigation | **A fourth product site carrying the same mixed-clock defect as F-066's originally-named sites: `computeBranchMemberAttendance` (`services/slot-engine/src/index.ts`), the admin-facing member-attendance roster.** It read an explicit `date` parameter as UTC while deriving the weekday from the process-local clock, and resolved each assignment's branch-local `startTime` through `slotStartForDate`, which pasted it straight into a UTC ISO string. The roster could therefore be **built for one calendar day and filtered by another** — an admin checking who is due today being shown a list assembled against a different day. F-066's plan inventoried the product sites after a systematic grep; this was the fourth, found only when the compiler flagged the changed helper signatures. **Same lesson as F-067's undercounted read-site audit, and worth stating as a pattern rather than a coincidence: a grep-based inventory of this codebase has now twice come in low, and the compiler has twice been what found the remainder.** | **Fixed as part of F-066 Stage 1 (`4665302`).** The branch timezone is resolved once from the `branchId` already in scope, then used for the date, the weekday (anchored at branch-local midday, so no DST transition can move it across a date boundary) and the window lookup. `dayBounds` was deliberately left untouched and a branch-aware `branchDayBounds` added alongside it, because `dayBounds` is shared with the occupancy endpoint that feeds availability generation — explicitly [[F-088]] scope. Covered by the byte-for-byte before/after no-op comparison, which included this endpoint's full response. |
| **F-083** | 13 Aug 2026 | 13 Aug 2026 | Found on the VM during today's deployment, reading Caddy's verbose startup logs line by line | **The `caddy` service had no `Caddyfile` volume mount, so Caddy silently used the copy baked into the image at build time and served plain HTTP with no automatic HTTPS.** Same class as the `GIT_SHA` gap found the same day — a compose file quietly missing something real — but with a worse failure signature: **there is no error path at all.** `docker compose ps` reported the service `running`, HTTP served normally, and the only trace anywhere was a single line buried in verbose startup output: `"server is listening only on the HTTP port, so no automatic HTTPS will be applied to this server"`. It was found by reading those logs by hand; nothing surfaced it. **Investigating for the fix disproved the initial framing and made the finding larger.** The gap was reported as a stale VM copy, so the fix was to add the mount. But the *committed* `deploy/gcp-vm/Caddyfile` begins `:80` — a bare port with no hostname — and `git log --follow` shows it has never changed since `c83a246` first added it. A bare port is exactly what makes Caddy skip HTTPS, so **mounting the repo file as-is would have mounted the broken config over the image's copy and made the failure permanent** — a fix that looks applied and isn't. Two separate defects had to land together: the missing mount, and a site address that has never carried a hostname. Confirmed the deployed VM file must differ, from production itself: `http://elitecourts.duckdns.org` returns `308` to `https://`, served under a real Let's Encrypt certificate (`CN=elitecourts.duckdns.org`, valid to 3 Nov 2026) — behaviour Caddy only produces for a hostname site address. Note the runbook **already stated** "Caddy's automatic TLS needs a hostname, not just a raw IP"; the knowledge was written down and the configuration never carried it. Compounding factor, shared with F-077: `caddy` is the heaviest image to rebuild (both Vite builds, against the recorded memory ceiling), so a `Caddyfile` that can only change by rebuilding is one that will not change. | **Fixed and verified red/green through the real committed compose file, not a hand-rolled `docker run`.** `Caddyfile` line 1 is now `{$SITE_ADDRESS::80}` — Caddy's env placeholder with `:80` as the default, so local and CI stacks are byte-for-byte unchanged while production sets the domain; `docker-compose.yml` gained `SITE_ADDRESS: ${SITE_ADDRESS:-:80}` and `- ./Caddyfile:/etc/caddy/Caddyfile:ro`; `.env.example` and the runbook's minimum `.env` gained `SITE_ADDRESS`. **RED**, compose exactly as committed at HEAD with `SITE_ADDRESS` set: container `running`, warning present once, HTTP `200`, HTTPS `status=000` — connection refused, no TLS listener at all. **GREEN**, same 2-hour-old image with **no rebuild**: warning count `0`, `enabling automatic HTTP->HTTPS redirects`, real TLS handshake, HTTPS `200` returning the bundle's own `version.json`, HTTP `308` to HTTPS. The mount was proven load-bearing independently — an image with `:80` baked in ignores `SITE_ADDRESS` entirely when unmounted and honours it when mounted. `docker compose config` confirms interpolation works through `--env-file`, the production path. **Remaining honest gap: no automated check would have caught the original state, and the one that comes closest still misdiagnoses it** — `deploy:verify` run against the `https://` URL fails every component when TLS is down, which is a real signal but reads as "the whole deploy failed" rather than "TLS is misconfigured". Worth a dedicated TLS assertion rather than relying on that side effect. |
| **F-053** | 7 Aug 2026 | 9 Aug 2026 | Found during F-044 (scheduler) design review — same severity class as F-045/F-048/F-049 | **HIGH SEVERITY — `POST /bookings/sweep` (`services/slot-engine/src/index.ts:2509`) has zero authentication.** Its handler has no request/reply params at all, while **6 other call sites** in the same file correctly use `requireInternalKey` (`index.ts:1893, 2068, 2120, 2680, 2738, 2827`). *(Corrected 9 Aug 2026 from an earlier "7 routes" — the original figure came from a `grep -c` that counted the helper's own definition line at `index.ts:100`.)* Caddy routes `/api/slot-engine/*` publicly. Net effect: **anyone on the internet can trigger the booking-release sweep with a single unauthenticated request** — real operational disruption potential, releasing genuine reservations at will. Same severity tier as F-045's original finding; found adjacent to unrelated work, not caused by it. | **Needs its own dedicated, prioritized kickoff — same urgency F-045 got, not deferred behind other queued work.** Likely fix: add `requireInternalKey` (or equivalent), matching the other 7 routes in the same file. **Fixed and committed as `c7404a2`.** |
| **F-061** | 7 Aug 2026 | 9 Aug 2026 | Found while verifying the `gracePeriodMinutes` validation gap | **TIER 1 CLOSURE CONDITION — DISCHARGED 11 Aug 2026 against the local stack.** All three rendered paths were driven through the real UI by Playwright (`apps/guest-member-pwa/tests/f061-browser-verification.spec.ts`), producing four screenshots under `test-results/f061/`: (1) **Admin Web Resources** — booking-rule save showing Guest Access Cutoff `135` and a rendered "Booking rule saved." confirmation, with `135` verified persisted by database read-back, exercising F-068's shared validator; (2) **Admin Web Refunds** — `cancel-preview` with an admin JWT rendering "Calculated tiered refund ₹500 (100%)", the load-bearing case for F-071's guard; (3) **guest PWA `CancelBookingModal`** — the modal rendered with its real refund breakdown, then the completed cancellation showing `CANCELLED`, exercising F-071's guard on the user-token path. Run against a purpose-seeded booking, cleaned up afterwards with read-back proof; no existing demo data consumed. **Deliberately run against the local stack, not the GCP VM, because the VM does not carry the Tier 1 fixes** — screenshots there would have evidenced pre-fix code, which for these paths is visually indistinguishable from the fix working. **Production re-verification after the VM is redeployed is tracked separately and is not part of this condition.** **HIGH SEVERITY, confirmed by a real unauthenticated request — `POST /booking-rules` (`services/slot-engine/src/index.ts:1394`) has zero authentication.** The handler takes only `request` (no `reply`) and calls neither `getInternalOrAdminAuth` nor `requireInternalKey`; slot-engine registers no global `addHook`/`preHandler` (verified by grep). Its sibling `PUT /resource-pools/:id/booking-rule` (`index.ts:1434-1436`) calls both `getInternalOrAdminAuth` and `requirePoolScope`. Caddy routes `/api/slot-engine/*` publicly. **Live evidence:** an anonymous POST with no `Authorization` header returned **HTTP 200** and created a rule with `prepaymentRequired: false` — removing the prepayment requirement for that pool — plus a 100%-refund-at-zero-hours cancellation policy, `lowOccupancyThresholdPct: 99` and `gracePeriodMinutes: 1`. The identical unauthenticated PUT returned **HTTP 401 `UNAUTHORIZED` "Missing authorization header"**, proving the gap is specific to this endpoint, not a platform-wide absence. **It appended rather than overwrote**: because `BookingRule` has no unique constraint on `resourcePoolId`, the pool went from one rule (`gracePeriodMinutes 30 / prepaymentRequired true`) to two conflicting rules (`30/true` and `1/false`), after which the unordered `bookingRules[0]` reads select arbitrarily and the pool's payment, refund and cutoff policy varies between requests. The same single request incidentally proved both the missing uniqueness constraint and the absent floor on `gracePeriodMinutes`. Injected row deleted and pool restored; test service stopped. | **Needs its own dedicated fix kickoff, same priority tier as F-045/F-048/F-049/F-053** — add `getInternalOrAdminAuth` + `requirePoolScope`, matching the sibling endpoint four lines away. Re-run the exact unauthenticated request as the regression proof: it must return 401. Sequence alongside F-053 as the two still-open HIGH-severity unauthenticated-write findings. **Scope the fix to include F-067 and F-068** — this endpoint is the door, F-067's missing uniqueness constraint is what turns one request into permanently ambiguous state, and F-068's absent validation is what lets that request carry a cutoff-inverting value. Closing the door alone leaves both reachable by an authorized admin mistake. **Fixed and committed as `6c5791f`.** |
| F-067 | 8 Aug 2026 | 9 Aug 2026 | F-044 Phase A design investigation; proven live during the F-061 `POST /booking-rules` auth test | **`BookingRule` has no uniqueness constraint or index on `resourcePoolId`** (`packages/database/prisma/schema.prisma:214-231`), **yet five separate read sites fetch `bookingRules[0]` with no `orderBy`** — `services/slot-engine/src/index.ts:552`, `:675`, `:1552`, `:2535`, `:2594` (across 13 total touch points). Postgres returns arbitrary order when multiple rules exist on one pool, meaning the effective `gracePeriodMinutes`, `prepaymentRequired`, and cancellation policy can differ between requests to the same endpoint. `PUT /resource-pools/:id/booking-rule` compounds this — an unordered `findFirst` followed by an upsert keyed on `existing?.id ?? '__missing__'` (`index.ts:1439`) means with two rows present, it updates an arbitrary one. **Confirmed live, not theoretical:** a single request during the F-061 auth test took one pool from one consistent rule (`gracePeriodMinutes 30 / prepaymentRequired true`) to two genuinely conflicting rules (`30/true` and `1/false`). This is the actual mechanism that turns F-061's auth gap from "overwrites a rule" into **"injects a permanently ambiguous second rule."** | Audit for existing duplicate rows before adding `@@unique([resourcePoolId])`; add explicit `orderBy: { createdAt: 'asc' }` at all five read sites as an interim mitigation. **Should be fixed alongside F-061, not independently** — the auth fix alone leaves this schema weakness exploitable by any legitimate admin mistake, not just an attacker. **Fixed and committed as `6c5791f`.** |
| F-068 | 8 Aug 2026 | 9 Aug 2026 | Surfaced while sizing F-044's poll interval against the real configurable range | **`gracePeriodMinutes` has no real floor and its two setters validate inconsistently.** `PUT /resource-pools/:id/booking-rule` accepts any integer `>= 0` including `0` (`services/slot-engine/src/index.ts:1451`). `POST /booking-rules` — **the unauthenticated endpoint from F-061** — applies no validation at all, and its `gracePeriodMinutes ? Number(gracePeriodMinutes) : 30` coercion (`index.ts:1413`) means an explicit `0` is falsy and silently becomes `30`, while a negative value like `-15` is truthy and stored as-is, placing the confirmation cutoff *after* the window starts and inverting every downstream `now >= cutoffTime` comparison. No database-level `CHECK` guards the column either. **Confirmed live:** `gracePeriodMinutes: 1` was accepted during the F-061 auth test. (The negative-value path is code-verified, not live-proven.) | Add a real minimum and align both setters to identical validation — this changes existing admin-configurable behaviour, so needs a product decision on the actual floor value, not just a code fix. **Treat F-061, F-067 and F-068 as one connected fix, not three independent backlog items:** F-061 is the door, F-067 is what lets a single request through it create permanently ambiguous state, and F-068 is what lets that same request carry a value that inverts safety logic once it lands. Fixing F-061 alone closes the door but leaves the other two structurally exploitable by any future mistake, authorized or not. **Fixed and committed as `6c5791f`.** |
| F-074 | 9 Aug 2026 | 9 Aug 2026 | Found and fixed during Track B's F-067 verification | **`POST /booking-rules` against a pool that already had a rule returned a raw Prisma `P2002` as a 500 — including the absolute filesystem path (`D:\apps\Platform\services\slot-engine\src\index.ts:1505:41`) and surrounding source lines.** The F-034/F-037 leak pattern, **newly introduced by F-067's own uniqueness constraint** rather than pre-existing: before the constraint the duplicate simply inserted, so there was no error to leak. Caught only because the auth verification happened to target an already-populated pool — a clean-slate test would have created the first rule successfully and never hit it. | Translated to `409 BOOKING_RULE_EXISTS` with an actionable message naming the correct endpoint (`PUT /resource-pools/:id/booking-rule`), verified with a real request returning that status and message with no path disclosure. Logged for the record because it is a real recurrence of an already-tracked pattern in brand-new code. **Standing lesson worth carrying: verify against realistic, already-populated state — a clean-slate test never reaches this class of defect.** |
| F-055 | 7 Aug 2026 | 7 Aug 2026 | F-044 Phase A, reopened after four of six tests were found structurally incapable of failing regardless of the real implementation | Phase A's original evidence was worthless, not merely thin — no migration existed, every test ran against an in-memory mock, and four tests were provably tautological (would pass identically if the real SQL were deleted). Five real code defects (broken failure isolation, a timeout releasing a lease on a still-running handler, `runJobNow` able to strip a different run's lease, an unread circuit-breaker counter, fragile timeout classification) plus one unapproved design addition were also found. | **Genuinely rebuilt, not just re-evidenced**: real migration added; all four tautological tests replaced with real Postgres-backed proofs, each confirmed via an actual red/green demonstration (the real implementation broken, the test shown to genuinely fail, then restored and shown to pass) — the strongest possible standard, applied because arguments about why a test *should* fail had already proven unreliable once. All five code defects fixed with real before/after evidence. The `skipIfDelayedBySeconds` design question resolved explicitly. Retry-by-status implemented (closing a previously undiscovered Finding D) with its own separate red/green proof, since it introduced a genuinely new claim mechanism. A reusable A-G peer-review report format was adopted mid-saga specifically to compress future rounds of this kind. Committed as `9906b99`. |
| F-048 | 6 Aug 2026 | 7 Aug 2026 | Found during F-045's fix work — same trust-boundary class, different field | HIGH SEVERITY — `isMemberBooking` was still a client-supplied field on `POST /bookings`, and controlled **four distinct effects**, not just the one originally flagged: (1) extended the browse-ahead window ~7d→~30d, (2) excluded the booking from `computePoolGuestOccupancy` — distorting the metric driving low-occupancy release, (3) surfaced the booking on the admin member-attendance dashboard, (4) made the grace-period sweep auto-release it as a no-show — **including a genuine, fully paid guest booking**, the single most serious consequence found. Investigation confirmed the field was genuinely vestigial as a request input: no production client ever sent it; the only app-side reference reads the stored value for a display label. | Removed entirely, not "correctly derived" — `POST /bookings` now always persists `isMemberBooking: false` and always applies the guest window. Confirmed real member bookings are unaffected (they're created via the separate, already-secured `ensureTodayMemberBooking` path, never `POST /bookings`). The sweep-auto-release risk specifically proven closed by directly creating a would-be-spoofed booking at the database level and confirming the sweep correctly treats it as a guest booking now. |
| F-049 | 6 Aug 2026 | 7 Aug 2026 | Found during F-045's fix work | HIGH SEVERITY — `POST /payments/create-order` trusted a client-supplied `amount`, never re-reading the authoritative value. Investigation confirmed no legitimate per-booking price override was ever removed by this fix — the only real override capability is per-*window* (admin-authored via F-043's patterns/overrides), unrelated to anything a booking request could ever send. | `create-order` now reads the authoritative amount from the `PaymentIntent` (itself derived from `booking.price`, which correctly includes any legitimate window-level price override). Client-supplied `amount` is silently overridden with the real value, with a traceable evidence log line (booking/intent ID plus both values) on any mismatch — proven with a real negotiated booking's `₹275` surviving untouched through create-order, and a separate spoofed request silently corrected. |
| F-052 | 2 Aug 2026 | 2 Aug 2026 | F-043 Phase C evidence review (independent UI-thread verification) | **Guest PWA showed stale availability slots for a closed-override date — a real race condition, not an evidence artifact.** The default-date availability request could resolve after the user had already switched to a closed date, overwriting the correct empty state with the earlier (wrong) slots. First surfaced because a screenshot (2 available slots shown) directly contradicted the server log (`availabilityReturned: 0`) for the same date/pool — caught by independently cross-checking evidence against itself rather than trusting either artifact alone. | `CourtBooking.tsx` now clears slots on new date requests and discards stale out-of-order responses. Re-verified with a matching screenshot (genuine empty state for the closed date) and a real evidence JSON showing internally consistent before/after/booking/closed-override data — including an unprompted cross-check linking the real booking's `windowId` to a specific lazily-generated window in the same evidence chain, stronger proof than originally required. Focused Playwright test, typecheck, and build all passed. This closes out F-043 Phase C's independent review — full pass. |
| F-045 | 6 Aug 2026 | 6 Aug 2026 | Found incidentally during regression-suite consolidation planning | HIGH SEVERITY — unauthenticated `userId` trust boundary spanned the entire core guest booking-and-payment chain. `POST /bookings` never verified the JWT at all; `userId` came straight from the request body. `POST /payments/intents` had no auth check whatsoever. `POST /payments/create-order` (register originally misnamed this `/payments/orders` — corrected here) verified *a* valid JWT but never cross-checked identity against `booking.userId`. **A second, subtler hijack was found during investigation**: even with `/bookings` and `/create-order` fixed, `/payments/intents` alone could still let an attacker create a payment intent against a *stranger's existing* booking — closed independently, not just as a side effect of the other two fixes. | All three endpoints now derive/verify identity from the JWT, silently ignoring any client-supplied `userId` (matching the proven `/member/today-assignment/confirm` pattern). Real spoofing attempts proven rejected (`403`) at all three endpoints. Full legitimate booking-through-payment flow proven still working end to end via real Razorpay checkout, including a real 3-person co-player booking (price only reaches the correct amount if the server genuinely accepted the co-players under the token-derived booker — proven twice, independently, through two different rendered-browser specs). Regression suite updated to assert the new correct behavior rather than describing the old vulnerability. Committed as `4c76d04`. |
| F-041 | 4 Aug 2026 | 5 Aug 2026 | F-035 Phase 2 | Member attendance for an upcoming window on the Overview dashboard — no visibility into which members were expected/confirmed | Built `computeBranchMemberAttendance` and a new branch-scoped endpoint, reusing F-022's cutoff/`memberAttendanceConfirmedAt` mechanics rather than inventing new logic. Five distinct states (`CONFIRMED`, `PENDING_CONFIRMATION`, `RELEASED_NO_SHOW`, `SUBSCRIPTION_INACTIVE`, `WINDOW_NOT_FOUND`) all proven with real evidence. **Independently re-verified in two rounds** (Antigravity + separate UI-review thread): first round found real gaps (no actual screenshots attached, an unverifiable PWA "before" state showing only a loading spinner, an unsubstantiated leak-scan claim); second round closed all of them — a genuine four-point same-session chain across both admin and member apps (phone/slot/time/cutoff matching throughout), a real inspectable leak-scan artifact (not just an assertion), and full auth-boundary coverage. First-round gaps were real and worth having caught, not rubber-stamped. |
| F-035 | 4 Aug 2026 | 4 Aug 2026 | Live GCP demo walkthrough, Phase 1 | No screen showed guest occupancy per court/branch — a core operational gap | Built `computePoolGuestOccupancy` shared helper, used by both a new branch-scoped Overview endpoint and the existing Low Occupancy endpoint (refactored onto it, not left separate). Real reuse proven — identical API responses for the same pool/date, not just similar. **Intentional side effect, documented plainly**: fixed a real pre-existing Low Occupancy denominator bug (`pool.capacity` → summed window capacity), with old-vs-new math shown explicitly (50% → 25% for the same seeded data). Trust-boundary, refresh, and empty-state edge cases all verified with real evidence, including a correctly-distinguished per-pool empty state (not a blank branch-level message) for a branch with pools but no windows. Phase 2 (member attendance) deliberately deferred, now tracked separately as F-041. |
| F-030 | 1 Aug 2026 | 2 Aug 2026 | UX/business-fit review, Admin journey | Admin Assignments table showed raw numeric day-encoding (e.g. `2,4`) instead of readable days (e.g. `Tue, Thu`) | Blocked on F-032 (Admin Web/Slot Engine Sunday-encoding mismatch), now fixed and committed. Day-label fix implemented on top of it and independently re-verified with a real before/after pair on the original flagged row (`Peelamedu Evening Courts`, `+919777777777`: `2,4 19:00` → `Tue, Thu 19:00`, cross-checked against the actual checkbox state) plus both F-032-verification rows (`Tue 16:00`, `Sun 16:00`). No layout regression across any capture. |
| F-032 | 1 Aug 2026 | 1 Aug 2026 | Discovered while planning F-030's display fix | **A real, previously-invisible functional bug: Admin Web's assignment form sent Sunday as `0`, while Slot Engine's actual weekday-matching logic used the ISO convention (`1=Mon...7=Sun`).** Meant any Sunday recurring member assignment created through the real Admin Web form would silently never match — every single Sunday, with no visible error — affecting both `GET /member/today-assignment` and the grace-period sweep. Monday-Saturday were unaffected since both conventions happened to agree on those six days; only Sunday was silently broken. | Fixed at the source (Admin Web's `weekdayOptions`, Sunday value corrected `0→7`) — not the backend, which was already internally ISO-consistent. Verified with real assignments on both Sunday and Tuesday, confirming the fix corrected Sunday without disturbing the six days that already worked; confirmed via real `GET /member/today-assignment` call returning `HAS_SESSION` on an actual Sunday. Checked for existing bad `0`-value rows in the database — none found, no backfill needed locally. |
| F-029 | 1 Aug 2026 | 2 Aug 2026 | UX/business-fit review, cross-journey | Internal/database-style user IDs (`ux-guest-review`, `ux-member-has-session`, `ux-member-no-assignment`, `admin-seed-member-002`) leaked into guest dashboard, member dashboard (two states), and Admin Assignments table | Raw IDs replaced with phone number in all four locations, verified with real before/after screenshots at each site (independently cross-checked pixel-by-pixel in a second review, not just claimed) plus a targeted leak-scan confirming all four originally-flagged strings are no longer visible. F-030 confirmed correctly untouched. **Coverage caveat worth remembering, not blocking**: verification covered the four specific instances originally flagged — whether this is complete for every row/member/state depends on whether the fix lives in a shared component or was applied per-screen; worth a quick sanity check next time that code is touched. |
| F-000 | 30 Jul 2026 | 30 Jul 2026 | Phase 9 | Refund override audit trail | `adminId` from JWT, full audit fields, tested |
| F-001 | 30 Jul 2026 | 30 Jul 2026 | Phase 9 (2nd pass) | Missing branch resource-pool listing endpoint | Added, verified with real assertion |
| F-006 | 30 Jul 2026 | 30 Jul 2026 | Phase 9 verification | Concurrency claim only tested sequentially | Real concurrent test added, DB constraint proven |
| F-007 | 30 Jul 2026 | 30 Jul 2026 | Phase 10 | Real login couldn't select any slot despite passing tests | Two real backend bugs fixed, closed with real incognito re-test |
| F-008 | 30 Jul 2026 | 30 Jul 2026 | Phase 10 | Real Razorpay UPI payment never tested | Real test account, real end-to-end UPI payment confirmed by user |
| F-010 | 30 Jul 2026 | 31 Jul 2026 | Phase 10 → follow-up round | Availability windows not aligned to clean hour boundaries | Backend rejects unaligned times (with actionable suggestion); UI companion fix (tappable time-grid replacing native clock) closed the loop in the Admin Web polish round |
| F-009 | 30 Jul 2026 | 31 Jul 2026 | Phase 10 → follow-up round | Co-player phone validation gap (client and server layers) | Fixed alongside F-010 in the same follow-up round — real Playwright evidence confirmed both client-side rejection and server-side validation working |
| F-015 | 31 Jul 2026 | 1 Aug 2026 | Admin Web planning → F-023 pre-check | No update/upsert endpoint existed for `ResourcePool`/`BookingRule` | Confirmed built via direct code verification during F-023 planning — resource-update and rule-update endpoints exist and function |
| F-016 | 31 Jul 2026 | 1 Aug 2026 | Admin Web planning → F-023 pre-check | Payment orchestration and member-assignment listing endpoints | Confirmed built via direct code verification during F-023 planning — `POST /payment-links/negotiated`, dual-path `GET /member-group-assignments`, and the low-occupancy release conflict guard all present and functioning |
| F-022 | 31 Jul 2026 | 1 Aug 2026 | Member self-confirm attendance phase | No screen existed for a member to see/confirm today's recurring slot | Built: `GET/POST /member/today-assignment(/confirm)`, JWT-derived identity, explicit cutoff-time eligibility gate, shared atomic booking helper reused by both confirm and sweep (deterministic `member-booking-{userId}-{windowId}-{date}` key). **Caught and fixed a real self-defeating bug during design review**: the sweep's existing auto-release logic would have silently un-confirmed a member's own valid confirmation — fixed by adding `memberAttendanceConfirmedAt`, with the sweep now only releasing bookings where this is still null. Verified with real concurrent-confirm race test (single booking ID, `raceCount: 1`) at both API and rendered-browser level, real screenshots of all three UI states, and a full clean production build across all affected packages including the shared `database` package. Committed as `f49c3fe...b118f1`. |
| F-011 | 30 Jul 2026 | (ongoing verification) | Post-Phase 10 | 10 commits landed without explicit sign-off | Accepted retroactively, rule tightened; F-012, F-013, and the Admin Web polish commits all show the rule holding since |
| F-012 | 31 Jul 2026 | 31 Jul 2026 | Validation sweep | Slot Engine failed real `tsc` build (`@fastify/jwt` undeclared) | Dependency added, build verified, proper sign-off |
| F-013 | 31 Jul 2026 | 31 Jul 2026 | Validation sweep | Concurrent refresh-token race (StrictMode double-invoke) | Request deduplication fix, real evidence, cross-tab residual risk noted separately |
| F-018 | 31 Jul 2026 | 31 Jul 2026 | Admin Web polish round, Resources screen | Resource Pools panel showed raw camelCase field keys as labels | Labels humanized, `pricingMode` enum confirmed from real code, verified with before/after screenshots |
| F-019 | 31 Jul 2026 | 31 Jul 2026 | Same round | `Caddyfile` showed dirty with no explanation | Confirmed as the legitimate `/admin` bare-path Caddy fix (internal rewrite), verified across all entry patterns and breakpoints, committed as its own signed-off commit |
| **F-091** | 14 Aug 2026 | 15 Aug 2026 | Found during F-090's investigation, not yet live-fire verified | **Four more unauthenticated state-mutating routes in the same file** (fixed; see resolution) (`services/slot-engine/src/index.ts`): `POST /resource-pools` (`:904`), `POST /resource-pools/:id/resources` (`:1013`), `POST /resource-pools/:id/availability-windows` (`:1031`), `POST /blocked-windows` (`:1726`). Same route-level auth map as F-090: 9 of 38 routes have no authentication, of which these four plus check-in are state-mutating. **The availability-windows route is notably worse-placed — every sibling availability route is correctly protected** with `getInternalOrAdminAuth` + `requirePoolScope` (patterns at `:1132`, `:1143`, `:1159`, `:1187`; overrides at `:1208`, `:1312`, `:1350`, `:1372`); only this one isn't. It is also the endpoint F-043 confirmed working by direct API call and the one F-087 concerns. **Also flagged, unauthenticated reads:** `GET /resource-pools/:id/occupancy` (`:1110`) and `GET /branches/:id/resource-pools` (`:1434`), while their branch-level equivalents `/branches/:id/guest-occupancy` (`:1392`) and `/branches/:id/member-attendance` (`:1415`) are both protected. (`GET /resource-pools/:id/availability` at `:1746` is intentionally public per BR-048; `/health` is not a concern.) **Source-verified only — deliberately not live-fire tested**, since proving these would mean writing real inventory data (pools, resources, bookable windows, capacity blocks) without sign-off first. Logged separately from F-090 rather than folded in, so one investigation's fix cannot silently absorb a different set of routes without its own verification. | **Fixed in `aea242f`.** All six routes guarded with patterns the file already proved correct, not new shapes: `POST /resource-pools` uses `getInternalOrAdminAuth` then `isAuthorizedForBranch` (no pool exists yet, so `requirePoolScope` has nothing to re-read); `POST .../resources`, `POST .../availability-windows` and `GET .../occupancy` use `getInternalOrAdminAuth` + `requirePoolScope`; `POST /blocked-windows` copies `POST /booking-rules` exactly, including validating the body id **before** the helper so it cannot query on undefined; `GET /branches/:id/resource-pools` is internal-key-or-JWT with the JWT tenant-scoped. **Live-fired both directions against `badminton_db_test`, never the demo database.** RED: every one of the four mutations genuinely succeeded unauthenticated with a database read-back proving the row existed, while `PATCH /resource-pools/:id` and `POST /booking-rules` returned 401 in the same run; both reads returned real occupancy figures and real pool names, capacities and pricing to an anonymous caller. GREEN: all six return 401, with the row confirmed **unchanged** on every rejection; internal key, owner JWT and same-tenant JWT succeed; wrong-branch manager and cross-tenant JWT are 403. **`POST /resource-pools` also stopped trusting the body for tenant identity**, following F-045 — an admin JWT supplies `tenantId` and the body value is ignored, proven by a request sending `tenantId: "SPOOFED-TENANT-SHOULD-BE-IGNORED"` whose pool landed under the token's real tenant; the internal key keeps the body value as the bootstrap path `provision-tenant.mjs` uses. `GET /branches/:id/resource-pools` needed more than a bare `jwtVerify`, since `:id` is caller-supplied and can name any branch in any tenant — it reuses F-071's mechanism, reading the tenant from the resource. A first cut guarded it with `jwtVerify` alone and rejected the internal service key, inconsistent with the dual-path `GET /bookings/:id`; **caught and corrected before regression**. **The branch-belongs-to-tenant check exposed a real fixture defect**: three suites had always created pools against `TENANT_ID`/`BRANCH_ID` that were never inserted as rows, so every fixture pool pointed at a non-existent tenant and branch. Fixed by seeding them, with `upsert` rather than `create` because the suites share those ids and none of them delete tenants. Also corrected a stale comment claiming pool occupancy was needed by guests — a caller sweep found no guest consumer at all. **Regression 57/57 across all five suites**, which also confirmed empirically that the additive `tenantId` on `AdminAuthContext` breaks no consumer. Whole-repo typecheck and build clean. Real browser pass: guest login through to a confirmed booking, and admin Overview, Resources and Low Occupancy rendering with 200s on the newly guarded routes. **Unblocks F-098** — FLOW-019 and FLOW-021 are authenticated and the create-pool contract is final, with branch a token-scoped choice rather than a free-text field. |
| **F-101** | 15 Aug 2026 | 15 Aug 2026 | Found during end-to-end demo walkthrough | **The regression suites wipe whatever database `DATABASE_URL` actually points at, unscoped.** `cleanDatabase()` in `services/tenant-management/src/regression/_fixtures.ts:37-54` issues `deleteMany()` across 13 tables — `tenant`, `branch`, `user`, `booking`, `resourcePool`, `roleAssignment` among them — with no `WHERE` clause and nothing tying the deletions to test fixtures. **Confirmed destructively, not reasoned about**: running `pnpm test:regression:tenant` during this session's own verification destroyed the entire JBC tenant, both branches, both pools, all resources, rules and patterns, *and* the pre-existing `courtowner1` tenant, in one command. Only `club1` survived, and only because the suite reseeds it. **Scope is wider than the originally-reported single file — verified across the repo: all five services do this**, with unscoped `deleteMany()` counts of 11 (identity-auth), 17 (notification), 14 (payment), 13 (slot-engine) and 14 (tenant-management), plus several more inside individual specs. `pnpm test:regression` is therefore five consecutive full-database wipes, not one. A sharper, unconditional version of F-046: that was specs colliding with each other; this is any regression run annihilating everything in the target database. **Given real demo/customer data now lives in this same database, this is an active standing risk, not a latent one** — a well-meaning "just run the tests before the demo" destroys the provisioned customer state with no warning and no indication anything happened. | **Fixed in `b0f8411`.** Guard added as `packages/test-harness/src/database-guard.ts` and wired into the `cleanDatabase()` of **all five** services, so every suite inherits it rather than one being patched. It **fails closed**: an unrecognised or unparseable database is refused rather than wiped, on the reasoning that a false refusal costs one environment variable while a false permit costs somebody's data. It reports the database *name* and never the URL, which carries the password. Behaviour: **REFUSED** for `badminton_db`, `prod_main`, and an unset `DATABASE_URL`; **ALLOWED** for names containing `test`/`ci`/`shadow`/`scratch`; **ALLOWED** for any database when `ALLOW_DESTRUCTIVE_DB_RESET=true`, with a warning naming what is about to be deleted. **Proven in both directions**: `pnpm test:regression:tenant` against the real `badminton_db` now aborts before any delete with the database name and both routes out, and the same suite passes 4/4 against `badminton_db_test` with the real database confirmed untouched afterwards. **Independently reproduced destructively twice** during the same session — once on discovery, and once more when the guard was tested before rebuilding so the suite ran the stale `dist` (the F-085 trap), which cost a third re-provision and removed any doubt the behaviour was real. Follow-up tracked as F-103: make the disposable database the default rather than something to remember. |
| **F-102** | 15 Aug 2026 | 15 Aug 2026 | Found during end-to-end demo walkthrough | **`apps/guest-member-pwa/src/components/BookingConfirmation.tsx:144` renders the literal string `"Coimbatore Badminton Hub"`** next to the MapPin icon. Every neighbouring field on that screen is dynamic — slot time (`{st} - {et}`), player count, membership type, total price — only the venue name is hardcoded. **Every booking confirmation, on every tenant and every branch, therefore shows this same fixed string regardless of which branch was actually booked.** Observed live during the JBC end-to-end walkthrough: a guest booked "Japan Badminton Court, Coimbatore" and the confirmation told them the venue was "Coimbatore Badminton Hub". Same white-label-leak class as the branding work, on the most customer-visible screen in the entire booking flow, and reached on the exact path the customer demo follows. | **Fixed in `b0f8411`.** `GET /branches/:id/about` now returns the branch `name` — a booking row carries `branchId` as a bare scalar with no Prisma relation, and no other endpoint returns a single branch by id, which is why the string had been hardcoded in the first place. `BookingConfirmation.tsx` resolves it in an effect **separate from the confirmation status poll**, so it runs once rather than every 1.5s, and a failure leaves the venue line **absent rather than wrong** — never show a venue that cannot be confirmed. Verified live on the final end-to-end walkthrough: the confirmation screen read "Japan Badminton Court, Coimbatore" for a booking at that branch, and `"Coimbatore Badminton Hub"` no longer appears anywhere on the page. Cross-checked per branch so the value is demonstrably data-driven rather than coincidentally right: branch 2 returns "JBC – New Japan Badminton Court" and the pre-existing branch returns "Coimbatore Main Arena". |
| F-099 | 14 Aug 2026 | 15 Aug 2026 | Found while scoping the branch location/reviews feature | **`BranchAbout.tsx:81-86` has rendered `aboutData.address` since the component was built, but `GET /branches/:id/about` (`services/tenant-management/src/index.ts:302-309`) constructed its response object with no `address` key at all.** Confirmed against the live endpoint before any change — the response carried description, facilities, photos, workingDays, workingHoursStart and workingHoursEnd, and no address. The address block on the branch detail page had therefore never rendered anything. Note the contrast that hid it: `GET /tenants/:id/branches` returns whole rows via `findMany` with no `select` (`:331-336`), so `BranchSelect.tsx:92-95` *did* show the address correctly — the field was visibly working one screen away from where it was silently missing. | **Resolved as a byproduct of the branch location/reviews feature**, not fixed separately — that work adds `address`, `latitude`, `longitude` and `googlePlaceId` to this exact response object, which is what the guest-facing "Get Directions" / "Leave a Review" links consume. Verified in a real browser after the change: the pre-existing `Coimbatore Main Arena` branch (no coordinates) now renders "Coimbatore, India" on its detail page where it previously rendered nothing, with both location buttons correctly absent and no `undefined`/`null` in any href on the page. |
| **F-090** | 14 Aug 2026 | 14 Aug 2026 | RE-document review → Track 1 verification, confirmed 14 Aug 2026 | **HIGH SEVERITY. `POST /bookings/:id/check-in` (`services/slot-engine/src/index.ts:2301`) had zero authentication — the only booking-scoped route in the file with no caller identity check at all.** The 23-line handler reads only the `:id` path parameter; its sole guard is a status check. Ruled out every indirect source of protection: `grep -c "addHook"` returns **0** (no `onRequest`/`preHandler`/`preValidation` anywhere in the file, so auth is strictly per-route), and `fastifyJwt` is registered at `:24-26` as a decorator only. **Bracketed on both sides by correctly-protected siblings** — `confirm` at `:2276` (`requireInternalKey`) and `cancel` at `:2324` (`requireInternalKey` \|\| `jwtVerify`, plus F-071's `requireBookingAccess` IDOR guard) — so this is a gap in one route, not a missing platform capability; 29 of 38 routes in the same file are authenticated. **Confirmed live**, built from current source against real Postgres: an unauthenticated `POST` returned `HTTP 200` with `"status":"CHECKED_IN"` while the control `confirm` on the same booking returned `HTTP 401`, and database read-back proved a genuine persisted write (`updatedAt` advanced 16s past `createdAt`). **Reproduced through the real public gateway path** — `Caddyfile:18-21` and `deploy/gcp-vm/Caddyfile` both proxy `/api/slot-engine/*` as a blanket wildcard with no path allowlist and no gateway-level auth, so this is reachable from the public internet on the live deployment, not only from inside the cluster. **Severity compounded by two factors.** (1) **Booking IDs are not secret in this system** — already leaked to users across five UI locations per F-029 (four) and F-037 (the pre-payment screen), so anyone who has seen a booking ID on a screen, screenshot, or support ticket holds a working attack parameter; the two findings compose. (2) **The mutation is irreversible through the API and blocks refunds** — no route transitions `CHECKED_IN` back (RE-006 evidences no reverse transition, and no admin status-override endpoint exists), and cancel accepts only `HELD`/`CONFIRMED` (`index.ts:2357-2360`), so flipping a booking permanently denies the legitimate customer their tiered refund and requires direct database intervention to undo. **This is a money-affecting, customer-facing denial, not just a data-integrity gap.** Tracked in the RE document set as VALIDATION-017 — the one finding family there with no decision surface anywhere, dispositioned `N/A` by both RE-011 and RE-012 through mutual deferral. | **Fixed in `0e94800`, merged to main.** Guard is `requireInternalKey` \|\| `requireUserJwt`, then F-071's `requireBookingAccess` — `cancel`'s dual-path pattern, **not** `confirm`'s internal-key-only one. That choice was settled against real callers rather than assumed: the sole caller anywhere is the guest PWA's "I'm Here" button (`apps/guest-member-pwa/src/components/BookingHistory.tsx:245`), which already sends the user's access token (`:41-44`), and `apps/admin-web` contains no check-in code at all — so internal-key-only would have broken the one working caller while appearing to fix a security hole. **Pre-flight cleared first**: `requireBookingAccess` fails closed without a `tenantId` claim (`index.ts:229`), and `services/identity-auth/src/index.ts:349-355` was confirmed to sign guest tokens with one. **Ordering is load-bearing** — identity is established before the booking lookup and `requireBookingAccess` runs before the idempotent early-return, so neither booking existence (404-vs-401) nor booking status is probeable; this follows the rule F-045 recorded at `index.ts:1856-1858`. **Verified 6 cases x 2 surfaces (direct + through Caddy), 12/12, with database read-back**: no auth header → 401 (was 200 — the regression proving the fix); booking owner → 200 + `CHECKED_IN`; different user, same tenant → 403; different tenant → 403; internal service key → 200; `branch_manager` on another branch → 403. The row was confirmed unchanged on every rejection. **slot-engine regression 28/28**, and the Playwright canary (`apps/guest-member-pwa/tests/guest-booking.spec.ts:137`, run individually per F-046) still passes — real browser, real OTP login, real JWT, self check-in through the gateway — so the guest flow was preserved with no client change. Scope was authentication only: F-091, F-092, F-093 and F-094 were logged separately and deliberately excluded, and the open product question about whether check-in should be staff-operated remains F-093. |
| **F-097** | 15 Aug 2026 | 15 Aug 2026 | Found during FLOW-059 extraction, payment service | **`POST /refunds` (`services/payment/src/index.ts:595`) had no caller authentication**, contrasted against its protected sibling `POST /refunds/override` (`:1045`, `jwtVerify` plus a role check returning 403) — same shape as F-090, materially lower severity. Indirect protection ruled out: the payment service registers exactly one hook, `preParsing` at `:31`, which exists to capture raw body for Razorpay signature verification, not to authenticate. The only `Authorization` header in the handler was the **outbound** one to Slot Engine at `:613`. Preconditions were narrow and attacker-uncontrolled — the booking must already be `CANCELLED` (`:631`), a `captured` PaymentIntent must already exist (`:646`), and `refundAmount` is read server-side from the booking (`:639`), never from the caller; the handler is idempotent (`:658-663`), so it could not be spammed into duplicates; and it moves no actual money, per INTEGRATION-FINDING-004 — refunds are local-only rows. **Real impact**: an unauthenticated caller who knew a booking ID (not secret, per F-029/F-037) could force-create the local Refund record ahead of the intended approval flow, pre-empting FLOW-060's override path, since once a Refund row exists the override handler's behaviour changes. | **Fixed in `000ab4b`.** Guard is `requirePaymentLinkAdmin` (internal key **or** owner/branch_manager JWT), **not** a copy of the override route's JWT-only guard — the two routes need different things: the override records an accountable `adminId` taken from the token, while this route records no `adminId` and makes no discretionary decision (it hardcodes `reason: 'customer_cancellation'` and reads the amount from the booking), so the platform/internal path must stay open for an automated cancellation-to-refund continuation. RE-003 lists both Admin and Platform as initiators, which is what the dual-path guard expresses. **Red/green, run against a nonexistent booking id throughout so no Refund row was ever written** — RED: no auth header → `400 BOOKING_NOT_FOUND` (reached business logic), sibling override → `401`. GREEN: no auth header → `401 UNAUTHORIZED`; internal service key → `400` (past auth, platform path intact); guest JWT → `403 FORBIDDEN`; owner JWT → `400` (past auth, admin path intact). **Pre-flight caught a break before it could cause a false failure**: the one existing caller, `services/payment/src/regression/autopay-and-refund.regression.ts:88`, sent no auth header and was updated to send the internal key — the same key it already sends to Slot Engine ten lines above. Payment regression 12/12. |
| **F-117** | 15 Aug 2026 | 15 Aug 2026 | Business requirement | **Multiple OWNER assignments per tenant already work, with no code change.** Three independent layers checked. (1) **Schema permits it**: `RoleAssignment` carries only `@@index([userId, tenantId])` (`schema.prisma:111`) — no unique constraint on tenant or role. (2) **The route permits it**: the only OWNER-specific rule in `POST /tenants/:id/roles` is that `branchId` must be null (`tenant-management/src/index.ts:391-398`); nothing checks whether the tenant already has an owner. (3) **Every consumer is membership-style, not identity-style** — all of them ask "does this caller hold owner", never "who is *the* owner of this tenant": `tenant-management:50`, `slot-engine:148`/`:179`/`:2775`, `payment:724`/`:864`/`:992`/`:1072`, `identity-auth:76`, `admin-web/main.tsx:220`/`:513`. **No `findFirst`/`[0]` singleton owner read exists**, so the F-067 failure mode (a non-deterministic effective record from an unordered first-row read) is absent here. A second owner inherits full cross-branch visibility by `main.tsx:513`, which is the intended semantics. | **Confirmed working as-is — closed with no code change.** Verified across three independent layers, all of which are re-checkable: the schema carries no unique constraint that could block a second owner (`schema.prisma:111`); the route's only OWNER-specific rule is that `branchId` must be null (`tenant-management/src/index.ts:391-398`), with no check for an existing owner; and every authorization consumer is membership-style, asking whether the caller holds owner rather than who the tenant's owner is. That last point was confirmed twice by separate sweeps — a targeted read of each call site, and an independent repo-wide grep of every `roles.includes`/`roles.some` site, which returned `identity-auth:76`, `payment:723`/`:863`/`:992`/`:993`/`:1071`, `slot-engine:147`/`:179`/`:180`/`:2775` and `admin-web/main.tsx:220` — **no site anywhere accepts a role beyond owner and branch_manager**. No `findFirst`/`[0]` singleton owner read exists, so F-067's non-determinism failure mode is absent. **This finding now constrains F-115**: its fix must use the `userId`-inclusive constraint `(userId, tenantId, branchId)` specifically to preserve multi-owner support — recorded on F-115 as a hard requirement, not a preference. |
| **F-115** | 15 Aug 2026 | 15 Aug 2026 | Found while investigating JBC admin provisioning | **`POST /tenants/:id/roles` promised an upsert and performed an insert** (fixed; see resolution). `services/tenant-management/src/index.ts:421` comments "Upsert the role assignment to avoid duplicate entries" but `:422` calls `prisma.roleAssignment.create`. `RoleAssignment` has only `@@index([userId, tenantId])` (`schema.prisma:111`) and no unique constraint, so nothing at either layer prevents the duplicate the comment claims to avoid. Re-running role assignment as a repeatable provisioning step would compound a row per run. **Correction to the original report — this has NOT manifested; the claim came from my own misreading and is withdrawn.** The two `club1` `BRANCH_MANAGER` rows on branch `481273aa` are **not** duplicates: they carry different `userId`s (`manager-1` and `cb689664-aa3c-44b4-939e-7a781b37a664`), which is a branch legitimately having two managers. A `GROUP BY userId, tenantId, branchId, role HAVING COUNT(*) > 1` over the whole table returns **zero rows** — there are no duplicates anywhere today. The defect is real and the comment/code mismatch is verbatim, but it is latent, not realised. | **Fixed in this commit.** The route now genuinely upserts, keyed on `(userId, tenantId, branchId)`, backed by a database unique index. **The index is `NULLS NOT DISTINCT`, and that is the whole point.** A plain `UNIQUE(userId, tenantId, branchId)` reviews as correct and *does not fix this bug*: Postgres treats NULL as distinct from itself, and every OWNER row carries `branchId = null` by the route's own validation, so the repeat-owner insert — the exact JBC re-provisioning case — sails straight through. Proven rather than reasoned: against a real table, the identical repeat-owner insert **succeeded** pre-migration and was **rejected** post-migration, while a second *distinct* owner still inserted cleanly (F-117 preserved). Postgres here is 16.14; `NULLS NOT DISTINCT` needs 15+. **The self-defending guard was exercised, not just written**: applying the migration while a duplicate was deliberately present aborted with the offending triple named, including `branchId=NULL`, and left the index uncreated — F-067's shape, reused rather than reinvented. **The typed Prisma upsert was tried first and cannot serve the OWNER path**: Prisma 5.14 generates the compound-unique input as `branchId: string`, not `string \| null` (`generated/client/index.d.ts:36429-36433`), so a null branch throws `PrismaClientValidationError` at runtime. It typechecked only because the route reads the body as `any` — which is precisely why this was runtime-tested rather than trusted. The route therefore uses a raw `INSERT … ON CONFLICT … DO UPDATE`, with a comment recording which path was taken and why; the returned row shape matches the Prisma model exactly, so the response contract is unchanged. `prisma migrate diff` confirms **no drift** between the schema's plain `@@unique` and the stronger real index, so the generated client and the database agree. **Regression 58/58 across all five suites**, including a new section asserting both halves: repeat assignment leaves exactly one row (F-115), and two distinct users still both hold OWNER (F-117). That test is also the tripwire if the index is ever regenerated from the schema alone and comes back plain. Whole-repo typecheck and build clean. **Verified end-to-end in the real path**: JBC's owner was provisioned and the assignment deliberately re-run — same row id both times, one row in the database — then a real admin-web login as that owner rendered Branches 2 / Active 2 with both JBC branches selectable. |
| F-020 | 31 Jul 2026 | 31 Jul 2026 | Admin Web polish round, mobile UX | Sidebar rendered full-width on mobile, pushing content below the fold | Collapsible nav built (820px breakpoint, overlay drawer, correct z-index layering above existing PWA overlay), desktop confirmed pixel-unchanged, verified with real screenshots and DOM measurements |

---

## Backlog (deliberately deferred, not bugs)

- **Cross-role flow diagram, after F-044 lands**: a new diagram distinct from the existing sitemap and guest-booking-flow diagrams — focused specifically on cross-role touchpoints (how an admin action becomes visible to a member/guest and vice versa), using F-023's proven integration scenario as the backbone. Explicitly include, as a discussion point: how an admin sets the rule for which guest bookings get included/counted (raised during F-043 planning, deliberately deferred to this later discussion rather than resolved now).

- Split-payment for group bookings (Premium)
- Per-branch training-hours actual timings — needs real data
- Staff training/rollout plan
- Member onboarding — WhatsApp migration path
- Real iOS device QA pass (see F-003)

---
*Started 30 Jul 2026, reconciled 31 Jul 2026 after an ID-collision incident. Only the reviewer assigns IDs going forward.*
