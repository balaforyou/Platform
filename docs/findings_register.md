# Findings Register — Running Log

**Purpose:** One live place tracking every gap, bug, or open decision surfaced during review — from the moment it's found until it's actually resolved. Modeled on the BCC governance-trail discipline: nothing gets fixed silently and forgotten, nothing stays open without being visible. Update this file the moment something is found, and again the moment it's closed — don't let resolution happen only in chat history where it's easy to lose track.

**How to use this:** Before starting any new phase, check this register for anything still `OPEN` that the new phase might touch. Before closing a phase, confirm nothing new got left `OPEN` without being logged here.

---

## Open

| ID | Found | Context | Description | Next step |
|---|---|---|---|---|
| F-002 | 29 Jul 2026 | Phase 8 (PWA installability) | 7-day dismissal-expiry logic never cleanly confirmed on real device — testing was confounded by the manifest-icon bug at the time | Deferred to Playwright automated coverage, to be built in the guest-booking phase |
| F-003 | 29 Jul 2026 | Phase 8 (PWA installability) | iOS install-instructions banner only confirmed via Chrome's iPhone emulation, never a real iOS device | Deferred until a real iPhone is available |
| F-004 | 30 Jul 2026 | Admin UI discussion | Production routing (Firebase Hosting rewrites mapping `/api/*` paths to each Cloud Run service) — never formally documented as a decision, only proposed. **Sub-item added 30 Jul 2026:** currently every backend endpoint is reachable directly by URL (no network-level restriction) — protection relies entirely on each endpoint's own auth check, which is correct standard practice (never rely on a URL being secret), but a real defense-in-depth option exists: Cloud Run's ingress setting can restrict a service to only accept traffic through the load balancer/hosting layer, rejecting direct hits to the raw `*.run.app` URL. Worth deciding whether to add this on top of the existing endpoint-level auth, not instead of it. | Parked, to be discussed and formalized together with the rest of production routing |
| F-005 | 30 Jul 2026 | Business discovery session 2 | Low-occupancy alert threshold — mechanism decided (percentage, admin-configurable, now a real schema field per Phase 9), but no default percentage value has actually been chosen | Needs a business decision on the default (e.g. 50%) |
| F-017 | 31 Jul 2026 | Admin Web Step 1 sign-off | All five Admin Web workflows (Branches, Resources, Assignments, Low Occupancy, Negotiated, Refunds) live in one 639-line `apps/admin-web/src/main.tsx`, unlike the guest PWA's pattern of one file per screen. This directly caused a real problem: the Step 1 fix (Branch/Resource save feedback) couldn't be committed as a clean whole-file scoped commit, since the same file also contains unverified, known-buggy Assignments/Negotiated code — required interactive `git add -p` patch staging to isolate just the approved hunks. Every future scoped commit and review on this file will face the same friction. | Worth splitting into per-screen component files before Step 3/4, matching the guest PWA convention — not urgent enough to block Step 1's commit, but should happen before the file grows further |
| F-014 | 31 Jul 2026 | Post-Phase 10 validation sweep (item 4) | Payment Service registers each Razorpay handler at two paths — `/create-order` (the real path real PWA traffic hits, after Caddy strips `/api/payment`) and `/payments/create-order` (used only by `payment.test.ts`, which bypasses Caddy and calls the service directly). Both work today, so this isn't currently broken — but `payment.test.ts` has never actually exercised the real Caddy-routed path, only the test-only duplicate. If the "unused-looking" bare-path registration were ever removed as apparent dead code, the test suite would keep passing while the real app silently broke — same blind-spot shape as F-007, caught proactively this time instead of after an incident. | Not urgent, doesn't block anything currently. Worth eventually either updating `payment.test.ts` to route through Caddy (so it validates the real path), or clearly documenting that it intentionally tests handler logic in isolation, not real routing. |
| F-011 | 30 Jul 2026 | Post-Phase 10 follow-up work | **Process violation, not a code bug.** 10 consecutive commits (`895f165` through `897a545`) landed without an explicit "commit it" sign-off from the user, despite the standing rule requiring it. Each underlying change was individually discussed and approved in substance during review, but the explicit trigger was skipped every time — including immediately after the rule was re-stated and the agent confirmed understanding it. Root cause: referencing the handover doc by path ("same working agreement as always") wasn't sufficient to prevent recurrence once already; the doc itself was also updated but a mid-session reference to it didn't stop the very next commit. | Accepted retroactively rather than rewriting history (each change was substantively approved, just not with the explicit trigger). Going forward: the sign-off rule is now restated inline, verbatim, in kickoff prompts rather than referenced by path only — reduces reliance on the agent re-reading a file mid-task. Watch closely on the next few phases to confirm this actually holds — F-012 and F-013's fixes both landed correctly with proper sign-off, a good early sign it's holding. |
| F-009 | 30 Jul 2026 | Phase 10, real-device manual test | No mobile number validation on co-player input — field accepted a 19-digit string. Needs checking on **two layers**: client-side UX validation (should reuse the same `normalizePhone` logic already built in Identity's OTP flow, not a separate inconsistent rule), and — more importantly — whether `POST /bookings`'s `coPlayers` array validates phone format **server-side**, given this project's established pattern that client-side validation alone is never sufficient (price/userType/adminId spoofing precedent). | Needs both layers checked and fixed; server-side is the one to verify carefully, not assume |
| F-010 | 30 Jul 2026 | Phase 10, real-device manual test | Availability windows are not aligned to clean hour boundaries (e.g. `10:16 PM – 11:46 PM` instead of `10:00–11:00`) — a real generation-logic bug, not cosmetic. Needs confirming whether this is only test/seed-data generation (lower stakes) or the same logic that would generate real tenant availability windows (higher stakes — must be fixed before any real branch goes live). | Find the generation logic, confirm scope (test-only vs. real), fix to snap start/end times to the hour |
| F-015 | 31 Jul 2026 | Admin Web phase planning | No update/upsert endpoint exists for existing `ResourcePool` fields (occupancy, pricing mode, duration) or existing `BookingRule` values — confirmed via `git grep` across full commit history for `server.patch('/resource-pools`, `server.put('/resource-pools`, and `resourcePool.update`, finding nothing. This is genuinely "build from scratch," not "extend an existing endpoint." | Decide before implementation whether Admin Web's phase includes building this new endpoint, or the branch/resource config screen is scoped to read/create-only for now |
| F-016 | 31 Jul 2026 | Admin Web phase planning | **Corrected 31 Jul 2026 after precise code verification** — original framing was wrong on 2 of 3 points. `POST /member-group-assignments` IS correctly dual-path (internal key or owner/branch-manager JWT), exactly as Phase 9 approved — no gap. `GET /member-group-assignments` is internal-only, but this was Phase 9's actual approved design (`"list, internal only"`), not a broken promise — it's a genuine *new* requirement Admin Web surfaces (a browser can't hold an internal key to list assignments), not a Phase 9 defect. `POST /bookings/negotiated` is internal-only by original design too — the code comment confirms it was always meant to be called *by Payment Service*, not directly by a browser, as part of an orchestration flow (browser → Payment Service, JWT-gated → Payment Service internally calls Slot Engine's negotiated endpoint with the internal key). **The real gap: that browser-facing orchestration endpoint in Payment Service doesn't exist yet** — `POST /payment-links` currently expects an already-created `bookingId`, with nothing yet tying negotiated-booking-creation and payment-link-creation together under one JWT-gated call. | Needs: (1) a dual-path or JWT-gated read variant of `GET /member-group-assignments` for Admin Web listing, (2) a new Payment Service orchestration endpoint (JWT-gated) that creates the negotiated booking and the payment link together in one call — this is new work correctly in scope for the Admin Web phase's backend prerequisites, just with corrected root-cause framing |

## Resolved

| ID | Found | Resolved | Context | Description | Resolution |
|---|---|---|---|---|---|
| F-000 | 30 Jul 2026 | 30 Jul 2026 | Phase 9 revision | Manual refund override audit trail — needed design (who/when/reason) | `adminId` sourced from JWT (not request body), full audit fields on `Refund` model, tested in Checkpoint 3 |
| F-001 | 30 Jul 2026 | 30 Jul 2026 | Phase 9 delivery (2nd pass) | `GET /branches/:id/resource-pools` never existed — guest booking's browse step depends on it | Endpoint added, public/no-auth, verified in Checkpoint 1 with a real assertion ("returns correct array of pools"), not just present in the route list |
| F-006 | 30 Jul 2026 | 30 Jul 2026 | Phase 9 verification (2nd pass) | Concurrency claim on `MemberGroupAssignment`'s partial unique index was only tested sequentially, not proving the DB-level race protection | Both sequential AND genuinely concurrent test cases now present as distinct assertions — concurrent case fires simultaneous requests and confirms exactly one 201 / one 409, proving the database constraint itself resolves the race |
| F-007 | 30 Jul 2026 | 30 Jul 2026 | Phase 10 | Real manual login could not select any slot at all, despite Playwright's full suite passing — the exact class of gap the manual real-device checkpoint exists to catch | Two real backend bugs fixed (Fastify reply status-pollution on dual-auth endpoints, a route path mismatch on payment intent creation). **Closed with real evidence**: user personally re-tested in a fresh incognito window (ruling out cache/session artifacts) — slot selection, per-person group pricing, and a real booking through to `CONFIRMED` status all confirmed working by direct observation |
| F-008 | 30 Jul 2026 | 30 Jul 2026 | Phase 10 | Real UPI Intent / Razorpay checkout had not yet been tested against a real Razorpay test-mode account | Real Razorpay test-mode account set up (Key ID, Webhook Secret rotated after an earlier exposure incident and stored correctly in `services/payment/.env`), webhook registered, real UPI test flow (`success@razorpay`) completed end-to-end by the user directly — booking confirmed via the genuine webhook path, not the mock |
| F-012 | 31 Jul 2026 | 31 Jul 2026 | Post-Phase 10 validation sweep | Slot Engine failed a real `tsc` production build — `@fastify/jwt` module/type declarations not found, `jwtVerify` missing on the request type in 4 places (the IDOR guards added in Phase 10). Invisible through every prior test because all runtime testing used `tsx` (transpiles without type-checking), never a real compile — would have silently blocked actual production deployment. Exactly the class of gap the validation sweep was run to catch. | `@fastify/jwt` added to `services/slot-engine/package.json` (matching Payment's version), build verified clean, committed only after explicit sign-off. Root cause of why it worked at runtime despite being undeclared was described as pnpm resolving it via a workspace-level install — though inconsistent with pnpm's default strict isolation and not fully reconciled; worth a future check of `.npmrc` for a `node-linker` setting. |
| F-013 | 31 Jul 2026 | 31 Jul 2026 | Post-Phase 10 validation sweep (item 3) | Concurrent refresh-token race — two `/auth/refresh` calls 3ms apart, first rotates the token, second gets `401` and forces logout. Confirmed with real millisecond-precision server logs, not just theory. Root trigger for the *reproduced* case: React 18 StrictMode double-invoking `AuthProvider`'s boot effect (dev-only, confirmed via code trace — `refreshSession` is a private, non-exported function called only from the boot effect and a 14-minute interval, nothing else). | Fixed via request deduplication (single in-flight promise) in `packages/ui-shared/src/context/AuthContext.tsx`. Verified with a real log line proving dedup fired, and the full Playwright suite run clean twice in a row. **Correction to initial "no production risk" claim**: the fix and the analysis are both scoped to a single browser tab/JS execution context. The httpOnly refresh cookie is shared across tabs on the same origin, but the deduplication variable is module-level (per-tab) — so two tabs of the same session (a genuinely ordinary scenario: home-screen install + a regular browser tab, or just two tabs open) could still race via the same underlying mechanism, unprotected by this fix. StrictMode-specifically is confirmed dev-only; the cross-tab path is not ruled out and remains a real, if lower-probability, residual gap. Not blocking — logged for a future robustness pass (Web Locks API or BroadcastChannel-based cross-tab coordination) rather than an immediate fix. |

---

## Backlog (not bugs — deliberately deferred scope, tracked so they're not forgotten)

- Split-payment for group bookings (Phase 2/Premium)
- Per-branch training-hours actual timings — needs real data from the owner, not a technical gap
- Staff training/rollout plan per branch
- Member onboarding — migration path from existing WhatsApp-based coordination
- Real iOS device QA pass (see F-003, same underlying gap, listed here too since it's a standing "before real launch" item, not just this one phase's checkpoint)

---
*Started 30 Jul 2026. Every future phase's review should check this file first and update it on close.*
